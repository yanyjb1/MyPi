# MyPi TUI 规模测试报告

日期：2026-09-26 · 代码版本：`18a983c` + 未提交工作树 · 机器：i5-13500H / 32 GiB / Fedora KDE

---

## 0. 先说结论

1. **TUI 渲染没有问题。** 8k 到 32k 条转录、120x40 终端、60 fps 输入压力下，**帧耗时恒定在 3.0 ms 均值 / 5–6 ms p99**，与转录规模无关；600 帧里 0–1 帧超过 16.67 ms 预算。渲染是窗口化的，代价只跟视口有关。
2. **冷启动没有问题。** 真实进程从 `exec` 到第一个上屏字节：**5.6–10.9 ms**，常驻 8.4 MB。
3. **内存模型**（第二轮修正后）：`RSS ≈ 5 MB 二进制/主题 + 5.7 MB syntect 语法集（一次性）+ 1.15 × 转录载荷 + 块缓存（3.5–16 MB）`。32k 条 / 35.6 MB 转录 → RSS 70 MB。
4. **真正的瓶颈在服务端，已修。** `Store::load_path` 的 SQL 是 **O(n²)**（8k 条 53 秒，32k 条约 14 分钟），而 TUI 的连接读超时是 15 秒，所以 `mypi attach` 一个超过约 4000 条记录的会话会直接报错退出。根因是外层 join 让 SQLite 选了平方计划；改成 `seq IN (SELECT seq FROM path)` 后 8k 条 **53 s → 40 ms**，32k 条 **约 14 分钟 → 150 ms**，结果逐行一致（§4）。
5. **第二轮改动已复测**（§5）：去掉 `dispatch` 的整段克隆；握手超时与读超时解耦（慢服务端不再表现为崩溃）；块预算 256 → 512（实测免费）；**另外在复测中揪出 wire 读路径上的两个 bug**——客户端逐行扫描是 O(n²)，以及 32 MB 的行长上限装不下 32k 条的转录。修完 32k 条的 attach 从"永远不出内容"变成 **0.29 秒**。593 个单测通过，clippy 无警告。
6. **一个此前没被认出来的固定成本**：syntect 的语法集在第一次渲染代码块时载入，**+5.7 MB**，与转录无关（§5.5）。第一轮报告里的内存模型是错的，它把这笔成本和克隆的常驻页都算成了"随转录增长"。
7. **第一轮的"首帧"指标是错的**（§5.4）：它量到的是状态栏，不是转录。改用"转录真正上屏"之后才发现 32k 条的会话**根本画不出内容**。教训记在这里：前端类改动的验收指标必须是"用户看到的东西出现了"，不是"进程写了第一个字节"。
8. **第三轮（§6）**：预算回到 256；测试改成**从底滚到顶再滚回**，实测缓存确实停在 256 块 / 9k–11k 行。顺带揪出并修掉一个滚动缺陷——**每帧代价原本正比于「离底部的距离」**（8k 条滚到第 36000 行时 94 ms/帧，11 fps），修复后是 4.0 ms 且与偏移无关。该轮把「满 256 块 = ~18 MB」当成缓存的代价，**第四轮证明这个读法是错的**（见 §7.5）。
9. **滚动耗时定论**：常态 **3.2–4.3 ms/帧**（滑过缓存 3.2–3.4，冷遍历未访问历史 4.0–4.3），与规模无关；峰值 **46–65 ms，全部出在 Ctrl-T/Ctrl-O 帧上**（全局折叠开关让所有块的变体失效），出现率 0.7–0.8%，不在滚动帧上。
10. **冷启动定论**：TUI 自己拉起 daemon（从零开始）**35–38 ms 到首帧**；daemon 已在跑时 attach 一个会话 **0.07–0.28 s 到转录上屏**（8k→32k）。
11. **「按 turn 渲染」的原因只有一个**（§7）：`ZoneView::on_stream` 是空实现，把服务端 ≤33 ms 一帧的流式快照扔了；wire 上一直是逐帧送的。接上之后（尾巴走与成品**完全同一条**渲染路径）真实进程实测：三块答复分别在 **0.42 / 0.52 s** 上屏（间隔 0.102 s，对应网关的 100 ms 分块），而修复前两块在**同一时刻**（间隔 0.000 s）出现；工具调用前那半句修复前**从未上过屏**。定稿前后屏幕逐行相同、答复只出现一次。
12. **`BLOCK_BUDGET` 是回溯深度旋钮，不是内存旋钮**（§7.5）：256 → 32 只省 **~4 MB**（32k：73.0 → 68.7 MB，尾部约 6%），耗时无回归。满缓存那 9k–11k 行本身只有 2–3 MB；「18 MB」是渲染时的分配器高水位。

---

## 1. 事实确认

用户要求先确认三条前提。逐条核对：

### 1.1 “TUI 侧就是一个纯 TUI，非流式模式下已经完全就绪”

**基本成立，有一处保留。**

- `run_tui`（`src/tui/session/loop.rs`）持有零会话状态：连接 daemon、收 `Signal`、交给 `App`/`SessionView`，自己不存转录、不存名字、不算用量。草稿态状态栏初值（模型显示名、cwd）由 TUI 本地播种，服务器活值到达后覆盖。
- 非流式路径（attach 快照 → 整段 `Transcript` → 渲染）端到端可用，本次测试全部跑在这条路径上。
- **保留（第四轮已补）**：`ZoneView::on_stream` 曾是空实现（`src/tui/session/view.rs`），即**流式渲染未接线**。第一至三轮都跑在非流式路径上；第四轮按用户要求把它接上了（§7）。
- 初测时 `mypi attach <id>` 在大会话上会因服务端 O(n²) 而超时崩溃（§4），修复后 32k 条 0.2 秒完成，这条路径现已端到端可用。

### 1.2 “server 与 tui 分离，中间有完好的翻译层，事件已规范/有模范样例，方便新建新的 UI”

**成立。**

- 翻译层是 `src/tui/session/view.rs`：`SessionView` trait（规范） + `MainSessionView`（唯一实例）。wire 消息 → `dispatch()` → `on_transcript/on_entries/on_stream/on_state/on_attached/on_error`。`dispatch` 的 match 是穷尽的，加新消息类型编译器强制补分支。
- 新前端只需实现 `SessionView`，TUI 代码零改动。翻译层本身不依赖 ratatui（只依赖 `Entry` 与 statusline 事件类型）。
- **一处小瑕疵**：`dispatch` 里 `ServerMsg::Transcript { entries } => view.on_transcript(entries.clone())`，而 `dispatch` 拿的是 `&ServerMsg`。这个 clone 是可以避免的（消息此刻已归读线程所有），代价实测 8k 条 4.2 ms、峰值内存瞬时可翻倍。

### 1.3 “仅对非流式进行测试即可”

已遵守。所有测量都在 attach 快照 / 完整转录路径上，未触发任何流式增量。

---

## 2. 测试方法

### 2.1 两个测试装置（新增，均在 `examples/` 下）

| 装置 | 作用 | 说明 |
|---|---|---|
| `examples/tui_scale_bench.rs` | 进程内全帧压测 | 合成转录 → 真实 `App` → `MainZone` → 子区 → ratatui 缓冲 diff → ANSI 编码（写入计数 sink，不走 syscall）。带计数分配器，能分别测「转录占用」与「缓存占用」；并先预热 syntect 语法集、单独报告那笔一次性成本。 |
| `examples/tui_real_bench.py` | 真实进程测量 | 隔离 XDG 根，起真 daemon，pty 里跑真 `mypi`，采 `/proc/<pid>/status` 的 VmRSS/VmHWM，分别量「状态栏上屏」和「转录真正上屏」两个时间点。 |

两个装置都只**读**现有代码，没有改动 `src/` 下任何一行。

### 2.2 合成数据

每个规模用固定种子生成，条目类型按随机权重混合：

| 掷点区间 | 占比 | 形态 |
|---|---|---|
| user | 22% | 3 段散文 |
| assistant（45% 的概率先产出一条独立 reasoning 条目） | 24% | 其中 25% 是完整 markdown（标题/列表/引用/rust 代码块） |
| 单次 tool_request + tool_result | 14% | 8 种工具名（bash/read/edit/write/grep/glob/fetch/search）；约 1/3 的结果超过溢出阈值，被替换成「巨物占位符」——**渲染器吃的是占位符，不是全文**，这是真实形态 |
| 连续工具调用突发（1–3 对） | 34% | 一个 agent 轮次的真实行为，工具量主要来自这里 |
| system 通知 | 4% | 左/居中对齐各半 |
| error | 2% | — |

体积标定对齐仓库既有约定（`gen_bench_db.py` 的 12 MB / 8k 条）：**约 1.1 KB/条**。

### 2.3 60 fps 压力模型

每帧一批随机事件（模拟 16.7 ms 内的输入到达），批次构成：

| 事件 | 概率 | 说明 |
|---|---|---|
| 滚轮 | 55% | 每次 1–3 步，向上偏置 62%，随机游走遍历整个文档（含冷缓存区段） |
| Ctrl-T | 12% | 全局折叠/展开思考链——**使全部块缓存变体失效** |
| Ctrl-O | 12% | 全局折叠/展开工具输出——同上 |
| 打字 | 19% | 1–3 个字符（含 CJK） |
| 退格 | 2% | — |

600 帧 = 10 秒的 60 fps 会话。

### 2.4 复现命令

```bash
cargo build --release --examples
# 流式尾巴的每帧代价：off（没尾巴）/ scrolled（尾巴在视野外）/ pinned（视野内）
./target/release/examples/tui_scale_bench stream 8000 4000

# 帧耗时 + 内存（每个规模单独进程，避免跨规模残留）
# 每档跑两个 profile：
#   full  从转录最底部滚到最顶部再滚回（每帧 60 行），这一档才会把 256 块缓存填满
#   mixed 随机游走（滚轮 + Ctrl-T/Ctrl-O + 打字），600 帧
for s in 8000 12000 16000 24000 32000; do ./target/release/examples/tui_scale_bench $s; done
# 终端尺寸敏感性
BENCH_COLS=80 BENCH_ROWS=24 ./target/release/examples/tui_scale_bench 32000
# 服务端 attach 成本
./target/release/examples/tui_scale_bench gen 32000 /tmp/x/mypi/sessions.db3
./target/release/examples/tui_scale_bench load /tmp/x/mypi/sessions.db3
# 真实进程冷启动 / 常驻内存
python3 examples/tui_real_bench.py draft 8000
python3 examples/tui_real_bench.py attach 32000

# 注意：tui_real_bench.py 会拉起 ./target/release/mypi，
# `cargo build --release --examples` 不会重建这个二进制——改过 src/ 后必须
# 先 `cargo build --release`，否则测的是旧二进制（本报告踩过一次）。
```

---

## 3. 结果：TUI 侧

### 3.1 冷启动（真实进程，pty，120x40）

`mypi` 草稿态（无会话），三次采样：

| 运行 | exec → 第一个上屏字节 | 常驻 RSS | 峰值 RSS |
|---|---|---|---|
| 1 | 7.1 ms | 8.4 MB | 8.4 MB |
| 2 | 10.9 ms | 8.4 MB | 8.4 MB |
| 3 | 5.6 ms | 8.5 MB | 8.5 MB |

包含：进程加载、配置解析、daemon 握手、主题初始化、`ratatui::init()`、第一帧全屏绘制。**与转录规模无关**（草稿态没有转录）。

### 3.2 转录注入 + 首帧（进程内，零拷贝）

走的是生产路径：`ServerMsg::Transcript` → `dispatch` → `replace_transcript`。第二轮把 `dispatch` 改成按值接收后，这条链上**一次复制都没有**（原先有一次整段克隆，见 §5.1）。

| 规模 | 载荷 | 注入 | 首帧（空缓存） |
|---|---|---|---|
| 8k | 8.7 MB | 0.0 ms | 3.9 ms |
| 12k | 13.3 MB | 0.0 ms | 5.2 ms |
| 16k | 17.8 MB | 0.0 ms | 5.2 ms |
| 24k | 26.6 MB | 0.0 ms | 3.9 ms |
| 32k | 35.6 MB | 0.0 ms | 4.8 ms |

注入是 0.0 ms：`Vec<Entry>` 只是被移进历史区，没有任何遍历。首帧是**常量级**：历史区窗口化，只渲染视口覆盖的块。32k 条的第一屏和 8k 条的第一屏一样贵。

### 3.3 60 fps 压力（600 帧，120x40）

| 规模 | 均值 | p50 | p95 | p99 | 最大值 | 超预算帧 | 600 帧墙钟（预算 10 s） |
|---|---|---|---|---|---|---|---|
| 8k | 2.90 ms | 2.94 | 4.06 | 5.04 | 18.2 ms | 1 | 1.77 s |
| 12k | 3.02 ms | 2.98 | 4.53 | 5.31 | 11.2 ms | 0 | 1.84 s |
| 16k | 3.01 ms | 3.07 | 4.08 | 4.72 | 34.5 ms | 1 | 1.83 s |
| 24k | 3.05 ms | 3.12 | 4.51 | 5.48 | 9.6 ms | 0 | 1.86 s |
| 32k | 3.13 ms | 2.80 | 4.91 | 6.05 | 37.3 ms | 1 | 1.90 s |

**帧耗时与规模无关**，墙钟只用掉 60 fps 预算的 19%。

按事件类型归因（32k）：

| 事件 | 帧数 | 均值 | 最大 |
|---|---|---|---|
| 滚轮 | 406 | 3.19 ms | 37.3 ms |
| Ctrl-T | 86 | 2.97 ms | 6.1 ms |
| Ctrl-O | 59 | 3.19 ms | 6.9 ms |
| 打字 | 49 | 2.85 ms | 4.9 ms |

**尖峰的归因**：600 帧里 0–1 帧落在 9–37 ms，出现在**滚轮帧**上，是滚进未缓存区段的冷缓存代价（一次要渲染若干整块）。Ctrl-T/Ctrl-O 触发的全局变体失效并**不**产生尖峰——变体切换只重算视口内的块。这 1 帧是一次性的，不构成持续掉帧。

第二轮把 syntect 语法集预先载入后（见 §5.4），最大值从 34.7 ms 降到 9.6–18.2 ms 的那几次运行，说明**首个代码块那一帧**原先还额外付了一次性成本。

### 3.4 终端尺寸敏感性（32k）

| 终端 | 均值 | p95 | p99 | 峰值 RSS | ANSI/帧 |
|---|---|---|---|---|---|
| 80x24 | 3.25 ms | 4.80 | 6.07 | 86.9 MB | 1.7 KB |
| 120x40 | 3.13 ms | 4.91 | 6.05 | 69.9 MB | 2.0 KB |
| 200x50 | 3.07 ms | 4.33 | 5.61 | 87.6 MB | 2.0 KB |

（80x24 与 200x50 两行是第一轮的采样，第二轮未重跑——尺寸本身不影响量级，只有视口内的行数在变。）

窄终端换行更多（行更多），但只影响视口内的行数，不改变量级。ANSI 输出 1.7–2.3 KB/帧 → 60 fps 下约 100–140 KB/s，对终端不构成压力。

### 3.5 内存

第二轮重测（syntect 语法集已预先载入，与转录无关的一次性成本单列）：

| 规模 | 载荷 | 块数 | 注入后堆 | 跑完 600 帧堆 | 块缓存增量 | 跑完 RSS | 峰值 RSS |
|---|---|---|---|---|---|---|---|
| 8k | 8.7 MB | 5 128 | 16.0 MB | 19.5 MB | 3.5 MB | 27.0 MB | 27.0 MB |
| 12k | 13.3 MB | 7 685 | 21.2 MB | 24.8 MB | 3.6 MB | 32.0 MB | 32.0 MB |
| 16k | 17.8 MB | 10 192 | 26.4 MB | 41.0 MB | 14.6 MB | 48.3 MB | 48.3 MB |
| 24k | 26.6 MB | 15 378 | 36.6 MB | 41.2 MB | 4.6 MB | 48.3 MB | 48.3 MB |
| 32k | 35.6 MB | 20 372 | 47.0 MB | 62.9 MB | 15.9 MB | 69.9 MB | 69.9 MB |

**内存与上下文的关系（核心答案）**：

```
RSS ≈ 5 MB（二进制 + 主题）
    + 5.7 MB（syntect 语法集，一次性，与转录无关）
    + 1.15 × 转录载荷字节
    + 块缓存（3.5–16 MB，受 512 块预算约束）
```

- **转录本身**：堆占用约 **1.15 × 载荷**（`String` 容量余量 + `Vec` 头）。模型与实测吻合到 3% 以内：`1.15 × 载荷 + 6.6 MB` 在 8k 给出 16.6 MB（实测 16.0），在 32k 给出 47.5 MB（实测 47.0）。
- **块缓存**：跑完 600 帧后堆增 3.5–16 MB。差异来自随机游走**最后驻留的是哪些块**——块的大小是重尾的（一行用户消息 vs 一个几百行的工具输出），所以同一个预算在不同路径下的占用可以差好几倍。它是固定上限，不随转录增长：32k 条的 20 372 块里只有 512 块以内的内容常驻。
- 第一轮报告里「RSS ≈ 8 MB + 2.2 × 载荷」的模型是**错的**，它把两笔一次性/固定成本混进了比例项：syntect 语法集（5.7 MB）和 `dispatch` 的无谓克隆（32k 条时约 37 MB 的常驻页）。第二轮修掉克隆、隔离语法集之后，模型才干净。

### 3.6 真实进程端到端（attach 一个已存会话）

指标是"转录真正出现在屏幕上"的时间，不是"第一个上屏字节"——后者只是状态栏，在转录解码完成前就已经画出来了（§5.4 就是因为这个区别才发现的两个 bug）。

| 规模 | 状态栏 | 转录上屏 | TUI 常驻 RSS | TUI 峰值 RSS | daemon 常驻 RSS | daemon 峰值 RSS |
|---|---|---|---|---|---|---|
| 8 000 | 0.08 s | 0.10 s | 34.7 MB | 34.7 MB | 43.4 MB | 43.8 MB |
| 16 000 | 0.14 s | 0.17 s | 45.0 MB | 45.0 MB | 72.3 MB | 73.5 MB |
| 24 000 | 0.19 s | 0.22 s | 55.7 MB | 55.7 MB | 100.9 MB | 102.6 MB |
| 32 000 | 0.26 s | 0.29 s | 66.5 MB | 66.5 MB | 129.8 MB | 132.8 MB |

进程内模型与真实进程吻合：32k 条预测 RSS ≈ 5 + 5.7 + 40.9 + 16 = 67.6 MB，实测 66.5 MB（真实进程、未滚动过、块缓存基本为空）。daemon 那一份更大是因为它还带着重建的模型上下文 `ChatContext`（约 40 MB）。

**daemon 与 TUI 各持一份完整转录**：32k 条合计约 196 MB 常驻。

---

---

## 4. 结果：服务端

### 4.1 修复前：attach 的全部成本集中在一个 SQL 上

8k 条 / 9.9 MB 载荷：

| 阶段 | 耗时 |
|---|---|
| `Store::open` | 0.5 ms |
| **`load_entries`（SQLite 读）** | **53 789 ms** |
| `entries_to_context`（重建模型上下文） | 5.4 ms |
| `transcript.to_vec()`（快照克隆） | 4.2 ms |
| wire 编码（9.9 MB 单行 JSON） | 10.3 ms |

除第一项外全是线性、毫秒级。**attach 的全部成本集中在一个 SQL 上。**

### 4.2 根因：`ORDER BY e.seq` 让 SQLite 换成了平方计划

`Store::load_path` 的外层查询原来是 join 形式：

```sql
SELECT e.seq, e.kind, e.payload FROM entries e
JOIN path p ON e.seq = p.seq
WHERE e.session_id = ?1 AND e.seq IS NOT NULL
ORDER BY e.seq
```

决定性实验——同一条 SQL，只去掉 `ORDER BY`：

| 变体 | 耗时 | 用户态 | 系统态 |
|---|---|---|---|
| `count(payload)`，无 ORDER BY | 0.011 s | 0.01 | 0.00 |
| 取全部 payload，无 ORDER BY | 0.030 s | 0.02 | 0.01 |
| 取全部 payload，**有 ORDER BY** | **53.158 s** | 37.50 | 15.30 |

`EXPLAIN QUERY PLAN` 的对比解释了原因。

无 ORDER BY —— 线性：

```
SCAN p                                          ← 外层：遍历 path
SEARCH e USING INDEX (session_id=? AND seq=?)   ← 内层：一次复合索引查找
```

有 ORDER BY —— 平方：

```
SEARCH e USING INDEX (session_id=?)   ← 外层：按索引顺序遍历 entries
SCAN p                                ← 内层：全表扫 path 的临时表（无索引）
```

`entries` 的主键索引是 `(session_id, seq)`。以 `entries` 为外层时索引的返回顺序**本身就是 `seq` 升序**，于是 `ORDER BY e.seq` 不需要排序。SQLite 认为「省掉一次 8000 行的排序」比「多扫 6400 万行」划算。而一旦 `entries` 成为外层，`seq` 的等值条件就无处可用（此时还没有 `p` 的行），索引约束从 `(session_id=? AND seq=?)` 退化成只剩 `(session_id=?)`；`path` 又是递归 CTE 物化出的**无索引**临时表，只能全表扫。

递归 CTE 本身没有问题（内层走的正是复合索引）。参数化绑定与字面量无关，Python `sqlite3` 用同一 SQL 复现同样 53 s。

单次内层访问成本（三点拟合）：

| 规模 | 总耗时 | 内层访问次数 | 每次 |
|---|---|---|---|
| 2 000 | 2.96 s | 4 M | 741 ns |
| 4 000 | 12.67 s | 16 M | 792 ns |
| 8 000 | 53.16 s | 64 M | 830 ns |

**问题不在常数，在次数是条目数的平方。** 8k 条的转录才 9.9 MB，一点不大——所以「8k 就崩」听上去荒谬，但成本与数据量无关，只与条目数的平方有关。阈值落在 4k 条左右（12.7 s 已贴近 15 s 超时）。

### 4.3 修复（`src/server/store.rs`）

1. 外层 join 改为集合成员判断，让 `seq` 重新成为索引约束：

```sql
SELECT e.seq, e.kind, e.payload FROM entries e
WHERE e.session_id = ?1 AND e.seq IN (SELECT seq FROM path)
ORDER BY e.seq
```

2. `effective_name` 原来调用 `load_entries`，把**整段转录**读进内存再倒着找最近的 `name` 标记——同一个平方查询在每次 resume 上跑第二遍。改成一条只取一行的 SQL（`kind = 'name'` + `ORDER BY seq DESC LIMIT 1`）。8k 条的真实 resume 成本因此从约 106 s（两次平方查询）降到约 40 ms。

3. 两处共用的递归 CTE 提取为 `PATH_CTE` 常量，注释里写明「外层不得用 join 形式」这个陷阱。

**正确性验证**（同一 DB、同一 leaf）：

| 查询 | 行数 | 结果哈希 | 耗时 |
|---|---|---|---|
| 修复前（全路径） | 8000 | `973fba1858a5ed53` | 53.345 s |
| 修复后（全路径） | 8000 | `973fba1858a5ed53` | **0.032 s** |
| 修复前（leaf=4000，replay 路径） | 4000 | — | 一致 |
| 修复后（leaf=4000） | 4000 | — | 一致 |

逐行完全一致。585 个单测通过，clippy 无警告。

顺带记录：同一批实验里「把 `e.seq = p.seq` 挪进 WHERE」和「加 `INDEXED BY` 提示」都**无效**（55.7 s / 54.4 s），只有 `IN (SELECT ...)` 真正改变了计划。

### 4.4 修复后：线性

| 条目数 | 修复前 `load_entries` | 修复后 | 加速比 |
|---|---|---|---|
| 2 000 | 2 963 ms | **10.6 ms** | 280× |
| 4 000 | 12 671 ms | **20.6 ms** | 615× |
| 8 000 | 52 894 ms | **39.6 ms** | 1 336× |
| 12 000 | （外推 119 s） | **58.7 ms** | — |
| 16 000 | （外推 212 s） | **77.9 ms** | — |
| 24 000 | （外推 477 s） | **111.9 ms** | — |
| 32 000 | （外推 848 s） | **150.5 ms** | — |

约 4.7 µs/条，干净的线性。

### 4.5 真实进程端到端（SQL 修复后）

| 会话规模 | 修复前 exec → 第一帧 | SQL 修复后 |
|---|---|---|
| 8 000 | 15.3 s 后**崩溃** | **0.1 s** |
| 32 000 | 约 14 分钟后超时崩溃 | **0.2 s** |

`mypi attach` 对长会话从「必然失败」变成「200 毫秒」。TUI 冷启动不受影响（草稿态 5.6–10.9 ms / 8.4–8.5 MB），TUI 帧耗时也不受影响（修复全在服务端）。

（内存数字见 §3.6：SQL 修复后 TUI 仍是 41.5 / 83.3 MB，第二轮去掉克隆才降到 32.5 / 45.9 MB。）

**新观察**：daemon 在 attach 32k 条时峰值 **167.6 MB**（载荷 39.1 MB，约 4.3×）。构成大致是：条目（约 45 MB）+ 重建的模型上下文 `ChatContext`（约 40 MB）+ 用于 wire 编码的 39 MB JSON 字符串，三者叠加。这条路径上服务端内存约为载荷的 3–4 倍，值得单独关注（不在本次修复范围内）。

### 4.6 关联问题：握手超时（已在第二轮修掉）

`ClientConn` 的 15 s 读超时原本与「握手可能长时间阻塞」的语义冲突。SQL 修复让 attach 从 53 s 降到 40 ms，**掩盖**了它但没有消除。第二轮把读超时降级为轮询间隔、另设明确的握手/快照截止时间，详见 §5.2。

---

## 5. 第二轮改动与复测

用户确认三条：**修克隆、修握手、块缓存设计保留**（只把上限拉大一点，等巨物机制重构完再看）。三项都做了，另外发现一笔一直没被认出来的固定成本。

### 5.1 `dispatch` 改成按值接收（去掉整段克隆）

**改了什么**：`view::dispatch(msg: &ServerMsg, …)` → `dispatch(msg: ServerMsg, …)`。所有权链条本来就是通的（读线程 → `Signal` → 循环变量 → dispatch → view），只是当初没传到底。顺带把 `Stream` / `State` 分支里每个 `String` 的克隆一并去掉——流式接线之后那是**每帧**的代价。

**复测**：

| 指标 | 改前 | 改后 |
|---|---|---|
| attach 8k：TUI 常驻 RSS | 41.5 MB | **32.5 MB** |
| attach 32k：TUI 常驻 RSS | 83.3 MB | **45.9 MB** |
| 克隆本身（保留为参考） | 17.3 ms @32k | 不再存在 |

**比预期大得多**：进程内测的堆只差 2–3 MB，真实进程的常驻内存却少了 **37 MB**。原因是克隆出来的那份立刻被丢弃，堆的 `live` 计数看不出差别，但分配器把那 37 MB 的页**留着没还给操作系统**——只有 RSS 看得见。这是"别只看 live 计数"的教训。

### 5.2 握手超时与读超时解耦

**改了什么**：把 15 秒从"截止时间"降级为**轮询间隔**，另设两个明确的截止时间：

| 常量 | 值 | 含义 |
|---|---|---|
| `READ_POLL` | 15 s | socket 读超时，只是"再看一眼"的节拍 |
| `HELLO_DEADLINE` | 10 s | hello 的回应上限（daemon 不碰库，慢就是坏了） |
| `SNAPSHOT_DEADLINE` | 300 s | 快照（attach）的回应上限，要装下一整个长会话 |

新增 `ClientConn::wait_for_within(pred, within)`：轮询超时不再直接失败，而是继续等到截止时间；到点了报 **`daemon 在 N 秒内没有回应`**，不再是 `os error 11`。`wait_for` 保留为"用快照截止时间"的薄封装，`quit` 用 hello 的。`loop.rs` 里读线程那两行重复的错误类型判断也换成了共享的 `wire::is_read_timeout`。

**测试**：新增 3 个（总数 585 → 588）。其中 `a_slow_reply_is_waited_for_not_treated_as_a_hangup` 在修复前**必然失败**（旧实现把一次 50 ms 的轮询超时当致命错误抛出）。

### 5.3 块缓存预算 256 → 512

按用户决定**保留"按块"的设计**：一块 = 一个对话单位，正是回滚想要的粒度；巨物机制上线后工具输出变成小占位符，块会小得多。

**复测结果：在本工作负载下，256 与 512 逐项完全相同。**

| 规模 | 预算 256 的堆 | 预算 512 的堆 |
|---|---|---|
| 8k | 19.0 MB | 19.0 MB |
| 12k | 24.3 MB | 24.3 MB |
| 16k | 40.5 MB | 40.5 MB |
| 24k | 40.7 MB | 40.7 MB |
| 32k | 62.4 MB | 62.4 MB |

结论：**预算不是当前的约束**——600 帧的随机游走驻留的块数从未达到 256，所以拉大上限在这里是免费的。它买的是真实使用中更长的滚动回看余量。

**反证实验**（确认缓存确实在干活）：把预算压到 **8** 块，32k 的帧耗时从 3.13 ms 涨到 **10.08 ms**，超预算帧从 1/600 涨到 **54/600**。缓存值这个钱。

注释也按实测改正了：原文自称「256 块 ≈ 1 MB spans，最坏约 7 MB」，实测在**没有巨物机制**的合成转录下是 9–21 MB。新注释说明了差异来源（巨物时代的块小得多），并明确预算是按"巨物时代"的块定的。

### 5.4 新发现：wire 读路径上还有两个 bug（都是复测时才暴露的）

§3.6 的数字最初是用"第一个上屏字节"量的——但**那只是状态栏**，TUI 在转录解码完成前就已经画出状态栏了。改用"转录真正出现在屏幕上"当指标后，两个 bug 立刻现形：

**（a）客户端逐行扫描是 O(n²)。** `ClientConn::read_msg` 每读一块 16 KiB 就用 `line_len(&buf)` 从**第 0 字节**重新找换行。一条 39 MB 的转录快照要读 2400 块，扫描量约 `len²/(2×chunk)` ≈ **46 GB**，实测就是几十秒的纯 `memchr`，期间屏幕上什么都没有。

修复：新增 `wire::LineScanner`，记住上次搜索放弃的位置，只扫新到的字节。daemon 侧的 `read_loop` 是同一个形状，一并改了。

**（b）`MAX_LINE_BYTES` 32 MB 装不下 32k 条的转录。** 32000 条的完整快照编码成**一行 39.1 MB** 的 JSON，超过上限，客户端直接拒绝——而报错文案还是"与 daemon 的连接已断开"，把人往"进程死了"的方向带（进程活得好好的）。

修复：daemon 把快照**分块**发出——第一块用 `Transcript`（替换），其余用 `EntryMany`（追加）。这正是 view 对这两种消息本来的语义，所以**协议不变、不用升版本号**。每块 2000 条（约 2.4 MB）。

顺带把那句撒谎的报错改成按错误类型分派：EOF/断管才说"连接已断开"，解码失败说"收到无法解析的消息"。

**复测**（真实进程，120x40，`attach` 一个已存会话）：

| 规模 | 转录上屏（修复前） | 转录上屏（修复后） | TUI 常驻 RSS | daemon 峰值 RSS |
|---|---|---|---|---|
| 8 000 | — | **0.10 s** | 34.7 MB | 43.8 MB |
| 16 000 | 3.46 s | **0.17 s** | 45.0 MB | 73.5 MB |
| 24 000 | 7.58 s | **0.22 s** | 55.7 MB | 102.6 MB |
| 32 000 | **永不**（报 `line exceeds MAX_LINE_BYTES`） | **0.29 s** | 66.5 MB | 132.8 MB |

分块还顺手削掉了一处内存尖峰：daemon 峰值 167.6 → **132.8 MB**（不再需要一次性构造 39 MB 的 JSON 字符串）。

TUI 那侧的数字要小心读：修复前的 82.5 MB 峰值是**解码失败**时的样子（39 MB 行缓冲 + 一次 39 MB 的 drain 拷贝，然后报错），当时它手里**一份转录都没有**；现在 66.5 MB 峰值是它**真的装着整段 35.6 MB 转录**的样子。也就是说峰值反而更低，而工作量从"什么都没拿到"变成"拿到了全部"。

**测试**：新增 5 个（593 总数）——两个 wire 层的（一条大消息分块到达、增量扫描 40 MB 保持线性），三个分块的（长转录切成块能逐条还原、delta 不会被当成替换、空快照仍要发一条）。

### 5.5 新发现：syntect 语法集是一笔 5.7 MB 的固定成本

`render/highlight.rs`：

```rust
static SYNTAXES: LazyLock<SyntaxSet> = LazyLock::new(SyntaxSet::load_defaults_newlines);
```

第一次渲染代码块时，syntect 把**全部默认语言**反序列化进一个进程级静态：**+5.7 MB**，与转录规模无关，一次性。

这解释了第一轮「跑 600 帧后堆涨 8–21 MB」里的一大部分——当时把它算进了块缓存。隔离之后（测试装置现在先预热它并单独报告），内存模型才干净：转录 1.15× 载荷、语法集 5.7 MB、块缓存 3.5–16 MB，三笔互不混淆。

顺带：预热后 32k 的最大帧耗时从 34.7 ms 降到 9.6 ms（该次运行），说明"首个代码块"那一帧原先还额外付了这笔成本。

---

## 6. 第三轮：预算回调、滚动测试，以及一个 O(离底部距离) 的滚动缺陷

用户三条指示：**预算回到 256**、把「7 MB」那个说法按实测改掉、并且**拟定一个能把 256 块用满的测试**（不然测出来的不是真正的内存峰值）。

### 6.1 预算回到 256，数字按实测改写

`BLOCK_BUDGET` 512 → **256**（撤回上一轮的调整）。注释里那个「256 块 ≈ 1 MB，最坏 7 MB」是错的，改成实测值：

| 规模 | 首帧后 RSS | 缓存填满后 RSS | 差值 |
|---|---|---|---|
| 8k | 23.7 MB | 40.9 MB | **17.2 MB** |
| 12k | 29.1 MB | 46.7 MB | **17.6 MB** |
| 16k | 33.9 MB | 52.2 MB | **18.3 MB** |
| 24k | 44.2 MB | 62.1 MB | **17.9 MB** |
| 32k | 54.9 MB | 72.9 MB | **18.0 MB** |

**满 256 块的代价是 ~18 MB，而且五个规模几乎一样**——这正是「按块数预算」的意义所在。注释当时写的就是这个数。

**这个读法在第四轮被推翻了**（§7.5）：把预算从 256 砍到 32，同样的遍历只省下 **~4 MB**。也就是说这 18 MB 主要是**渲染时分配器留下的高水位**（遍历要把五千到两万个块画一遍，画的当下申请、之后释放，页留在进程里），**不是缓存自己驻留的行**——缓存那 9k–11k 行本身只有 ~2–3 MB。注释已按此改写：块数是**回溯深度**旋钮，不是内存旋钮，两个方向都不该拿它去修 MB。

### 6.2 能填满 256 块的测试

上一轮的随机游走**从来没让驻留块数接近 256**，所以它测的不是真正的峰值。这一轮加了 `full` 档：

- 从转录**最底部一路滚到最顶部，再滚回底部**（每帧 20 个滚轮事件 = 60 行），中途照旧随机插 Ctrl-T / Ctrl-O / 打字；
- 走完一趟（1 lap）就停，帧数上限只是保险丝。

测试装置每档结束都会打印缓存的实际占用，所以"填满了没有"是**读数**，不是假设：

```
[full ] ... cache 256 blocks / 11274 rows (budget 256)
[mixed] ... cache 256 blocks /  9821 rows (budget 256)
```

五个规模都稳定停在 **256 块 / 9k–11k 行**。

### 6.3 顺带揪出一个滚动缺陷：每帧代价正比于「离底部的距离」

第一次跑 `full` 档就发现：8k 条、滚到第 36000 行时**每帧 ~94 ms（11 fps）**，而且越往上滚越慢。逐帧实测（8k，带缓存占用）：

| 帧 | chat_scroll | 最近 50 帧均值 | 缓存 |
|---|---|---|---|
| 100 | 6 000 | 14.9 ms | 256 块 / 9 879 行 |
| 200 | 12 000 | 54.1 ms | 256 块 / 9 823 行 |
| 300 | 18 000 | 64.3 ms | 256 块 / 9 975 行 |
| 400 | 24 000 | 96.3 ms | 256 块 / 9 336 行 |
| 600 | 36 000 | 94.3 ms | 256 块 / 9 924 行 |

约 **2.5 µs × 离底部的行数**，与视口高度无关。

**根因**：`window_from_bottom` 的倒走查从转录底部一路走到视口，对**经过的每一块**都调 `ensure_measured`。而 `ensure_measured` 只要发现该块的 `Range` 不在 `slots` 里就**重新渲染**——缓存只有 256 块，走查却要经过几千块，于是每帧把这几千块重画一遍。代码注释当时写的是「这一块在请求的区间内，行马上就用得上」——那个假设不成立：走查经过的绝大多数块根本不在视口里。

**修复**：新增 `height_or_measure`。走查只需要**高度**；高度备忘里有时直接返回，不出图。视口内那几块的行仍由 `rows_for` 按需渲染。备忘里没有的块照旧渲染并进缓存，所以缓存该填满还是会填满。

**修复后**（同一档、同一规模）：

| | 修复前 | 修复后 |
|---|---|---|
| 8k 走一趟的帧均值 | ~94 ms（且随偏移增长） | **4.03 ms**（与偏移无关） |
| 8k 走一趟的墙钟 | 无法在合理时间跑完 | 8.75 s（60 fps 预算 36.13 s） |

### 6.4 第三轮结果

**冷启动 A —— 从 server 开始**（没有任何进程在跑，TUI 自己拉起 daemon 并等它应答）：

| 规模 | exec → daemon banner | exec → 首帧 | TUI 常驻 | 峰值 |
|---|---|---|---|---|
| 8k–32k | 8.9–11.3 ms | **34.9–37.6 ms** | 8.3–8.6 MB | 8.3–8.6 MB |

daemon 的 bind 不碰数据库（会话懒加载），所以 banner 在 10 ms 内就出来了；TUI 的等待是它的 25 ms 轮询节拍。**与转录规模无关**。

**冷启动 B —— 从 attach 开始**（daemon 已在跑，附着一个已存会话）：

| 规模 | 状态栏 | 转录上屏 | TUI 常驻 | daemon 常驻（峰值） |
|---|---|---|---|---|
| 8 000 | 0.07 s | **0.10 s** | 34.5 MB | 43.2 MB（43.7） |
| 12 000 | 0.10 s | **0.13 s** | 40.3 MB | 57.6 MB（58.4） |
| 16 000 | 0.13 s | **0.16 s** | 45.1 MB | 72.0 MB（73.5） |
| 24 000 | 0.19 s | **0.22 s** | 55.8 MB | 100.8 MB（102.9） |
| 32 000 | 0.24 s | **0.28 s** | 66.9 MB | 129.6 MB（132.8） |

**60 fps 每帧耗时 —— `full` 档（从底滚到顶再滚回，缓存真正填满）**：

| 规模 | 帧数 | 均值 | p50 | p95 | p99 | 峰值 | 超预算 | 墙钟（预算） |
|---|---|---|---|---|---|---|---|---|
| 8 000 | 2 168 | 4.03 ms | 3.72 | 5.28 | 12.20 | 54.19 ms | 16 / 2168 | 8.75 s（36.1） |
| 12 000 | 3 245 | 4.13 ms | 3.81 | 5.47 | 12.41 | 46.86 ms | 28 / 3245 | 13.43 s（54.1） |
| 16 000 | 4 316 | 4.17 ms | 3.87 | 5.49 | 13.88 | 51.62 ms | 39 / 4316 | 18.01 s（71.9） |
| 24 000 | 6 496 | 4.26 ms | 3.95 | 5.68 | 12.39 | 61.47 ms | 46 / 6496 | 27.76 s（108.3） |
| 32 000 | 8 604 | 4.32 ms | 3.98 | 5.69 | 12.42 | 65.02 ms | 70 / 8604 | 37.30 s（143.4） |

**60 fps 每帧耗时 —— `mixed` 档（随机游走 600 帧）**：

| 规模 | 均值 | p50 | p95 | p99 | 峰值 | 超预算 |
|---|---|---|---|---|---|---|
| 8 000 | 3.24 ms | 3.25 | 3.53 | 3.62 | 3.81 ms | 0 / 600 |
| 12 000 | 3.24 ms | 3.28 | 3.56 | 3.67 | 4.92 ms | 0 / 600 |
| 16 000 | 3.22 ms | 3.29 | 3.57 | 3.70 | 4.05 ms | 0 / 600 |
| 24 000 | 3.31 ms | 3.31 | 3.64 | 3.96 | 5.20 ms | 0 / 600 |
| 32 000 | 3.35 ms | 3.36 | 3.68 | 4.02 | 5.29 ms | 0 / 600 |

**常态与峰值的结论**：

- **常态（滑过已缓存的内容）**：3.2–3.4 ms/帧，与规模无关，`mixed` 档 600 帧**一帧都没超预算**。
- **常态（冷遍历未访问过的历史）**：4.0–4.3 ms/帧——比前者贵约 25%，但同样与规模无关（这是修复后的数字；修复前它是 94 ms 且随偏移增长）。
- **峰值**：46–65 ms，**全部出现在 Ctrl-T / Ctrl-O 帧上**，即"折叠/展开全局开关"——它让所有块的变体失效，视口内那几块必须重画。出现频率 0.7–0.8%（16/2168 … 70/8604），而且与滚动位置无关（`scroll=5160` 到 `scroll=206940` 都有）。单帧 60 ms 相当于一次 16 帧的顿挫，肉眼可见但不影响操作。
- 峰值**不在滚动帧上**——滚动最差的一帧是 40.75 ms（24k），仍是冷缓存测量那一类。

**内存峰值与常态**：

| 规模 | 载荷 | 首帧后（常态） | 缓存填满后（峰值） | 堆 live（峰值时） |
|---|---|---|---|---|
| 8 000 | 8.7 MB | 23.7 MB | 40.9 MB（41.3） | 34.0 MB |
| 12 000 | 13.3 MB | 29.1 MB | 46.7 MB（47.1） | 39.3 MB |
| 16 000 | 17.8 MB | 33.9 MB | 52.2 MB（52.4） | 44.5 MB |
| 24 000 | 26.6 MB | 44.2 MB | 62.1 MB（62.8） | 55.2 MB |
| 32 000 | 35.6 MB | 54.9 MB | 72.9 MB（73.4） | 66.2 MB |

峰值 − 常态 = **17–18 MB**，就是那 256 块。

### 6.5 关于「非流式模式 server 一次返回本 turn 的所有内容」

**没有改。** 用伪网关实测了 buffered 模式下一条真实 turn 的 wire 序列：

```
0.00s  hello_ok
0.01s  attached
0.01s  stream       active=True text='我先看看'
0.31s  transcript   3 entries: [user, tool_request, tool_result]
0.31s  stream       active=True text='读'
0.42s  stream       active=True text='读完了'
0.52s  stream       active=True text='读完了。'
0.62s  entry_many   1 entries: [assistant]
0.62s  state        busy=False
```

**结论先行：wire 上其实已经是逐块送的，"一次给完"是 TUI 不画中间帧造成的错觉。**

1. **条目本来就是逐条送的**：用户消息、工具请求、工具结果各自入转录、各自发 `Change`，只是它们若落在**同一个 5 ms pump 节拍**里会被合并成一条消息。上面那次工具请求和工具结果就是被合并的（`transcript 3 entries`）；工具慢一点就会分成两条。

2. **助手回复也是逐句送来的**：`stream` 帧 `'读'` → `'读完了'` → `'读完了。'` 每隔 100 ms 一条，正是网关吐字的节奏。**但 TUI 的 `SessionZone::on_stream` 是空实现**（§1.1 那个"流式渲染未接线"），前端把中间帧吃掉了、只画最终落地的 `Entry::Assistant`。所以用户看到的是"整段一次到达"，而 wire 上它一直是分帧的。

3. **顺带发现 buffered 模式是死的**：`Session::set_stream_mode` **没有任何调用者**——`cfg.app.streaming`（以及 `MYPI_STREAM_MODE`）解析出来了，但从来没传进会话，会话字段停在 `Immediate` 默认值。所以今天**只有一种模式在跑**，`MYPI_STREAM_MODE=buffered` 是无效配置。

**要"返回块的内容"的话，难度和破坏性取决于想要哪一种**：

| 解释 | 改动 | 难度 | 破坏性 |
|---|---|---|---|
| A. 让"块"在 wire 上成为一等单位（每条消息 = 一个完整对话块） | daemon 要知道"块"的边界。分组逻辑现在在 TUI 的渲染层（`blocks::blocks`），而架构明确禁止服务端知道终端的事（`lib.rs` 的 `architecture` 测试在守着这条线） | **大** | **高**：要么把分组搬到服务端（破坏分层），要么让服务端复制一份分组规则（两处真相）。**而且没必要**——前端已经拿到逐条条目，块是渲染概念，理应在渲染层 |
| B. 补上 `on_stream`（把半句拼进历史区） | TUI 侧，就是 SERVER.md 里排着的 live tail | 中 | 低。**这才是"看起来一次给完"的真正原因** |
| C. 让 buffered 真正生效（`SessionSpec` 加字段 + `hub.assemble` 调 `set_stream_mode`） | 约 10 行 | **小** | **无**：`set_stream_mode` 本来就存在，只是没人调 |

**我没有改 A。** 理由：wire 上本来就没有"一次给完"这回事，改成以块为单位要动分层，收益还是负的（前端拿到逐条条目已经够了）。真正缺的是 **B**——`on_stream` 空实现，这才是"整段一次到达"的来源；和 **C**——一个解析了却没接线的配置项。B 是 SERVER.md 里既有的计划项，C 是十行。**等你醒了说一句要哪个。**

### 6.6 小发现：daemon 的 banner 会漏进 TUI 的终端

`connect_daemon` 拉起 daemon 时只把 stdin 接到 null，stdout 是继承的，于是 `mypi daemon listening on /tmp/.../mypi.sock` 直接写进 TUI 的终端（在 alt-screen 之前，闪一下就没了）。冷启动测量里它出现得比首帧还早，一度让我的指标失真。修法是 spawn 时把 stdout 也接到 null。没改——一行的事，但属于"你没让我动"的范围。

---

## 7. 第四轮：流式 live tail —— 「按 turn 渲染」到底是什么原因

用户问：**TUI 现在是按 turn 渲染的，原因是什么？** 然后要求「先修一下」。

### 7.1 原因：唯一一个，就是 `on_stream` 是空实现

代码里只有一处决定这件事：

```rust
// src/tui/session/view.rs（修复前）
fn on_stream(&mut self, _frame: StreamFrame) {
    // Live tail 渲染（流式那半句拼进历史区）是下一步的活：协议与
    // 翻译层已就位，渲染策略另做。此处先吃掉消息，避免上层误判丢帧。
}
```

也就是说：

- **wire 上一直是逐帧送的**：daemon 把「正在流的那半句」按 ≤33 ms 的节拍合并成 `stream` 消息推给前端，消息里带的是**整个缓冲区**（不是增量），外加 `reasoning` / `live` / `run_state`。
- **TUI 把它扔了**。历史区只从 `push_entry` 拿到定稿的 `Entry`，而定稿只发生在 `SessionState::finalize_round`（回合结束）。所以屏幕上唯一会变的时刻就是回合结束——**一回合一次，看起来就是"整段一次到达"**。
- 顺带发现 **`Session::set_stream_mode` 没有任何调用者**：`config.yaml → app.streaming` 与 `MYPI_STREAM_MODE` 解析得出来，但从来没传进会话，字段停在 `Immediate` 默认值。所以「非流式」其实从未生效过（§6.5 选项 C，仍未做）。

所以「非流式 TUI 按 turn 渲染」这个说法要反过来读：**服务端一直在流式送，前端一直没画**。

### 7.2 修法：尾巴走和成品**完全同一条**渲染路径

关键约束是「回合结束不能跳」：流式那半句一旦被正式条目替换，画面必须一模一样。做法是让它**就是**同一条路径的产物：

| 环节 | 做法 |
|---|---|
| 数据 | `HistoryZone::set_live(reasoning, text)`：按值移动（`dispatch` 已经按值，零拷贝），存进私有的 `LiveTail`。**不进 `entries`、不进块缓存、不进「代」的账**。 |
| 出图 | `chat::live_tail()` 调 `render_block(kind, text, Streaming::Live, width, t)` + `blocks::wrap_rows` —— 与 `single_node` 逐字节同路，只是显式声明 `Streaming::Live`。顺序也照 `finalize_round`：先 `Reasoning` 后 `Assistant`，中间一条间隔，空的那半不出图。 |
| 切窗 | 尾部那段行数在切窗**之前**从视口扣掉（`DELETED.md` 记的那条铁律），扣剩的才是转录能用的行数；偏移滚到尾巴上方，尾巴整段落在视口下方、自然不参与。 |
| 锚定 | 未跟随时，尾巴长高多少行就把偏移补上多少行——否则读者往上翻着读，字会一格一格把画面顶走。跟随（偏移 0）时相反：要的就是看着字往上顶。转录本身动了（追加/替换）不补偿，那种一跳本来就有。 |
| 开销 | 尾巴行按 `(版本, 宽度, 思考折叠, 主题代)` 备忘。帧是整缓冲区的重发，内容没变就不重新折行（markdown + 语法高亮按字符数算钱）。 |

`Streaming::Live` 这个枚举本来就在（注释写着「Live 绝不进缓存」），`render_block` 对 Live/Final 也早就画法相同（注释写着「未闭合的围栏照常画」）——**缺的从来不是渲染，是接线**。

### 7.3 验证：真实进程，pty，逐帧回读屏幕

压测装置用最小 ANSI 模型（只处理 ratatui 会发的 CUP/SGR/EL/ED/备用屏）把 pty 字节流还原成屏幕，再看**每个字第一次出现在屏幕上是什么时候**。伪网关第二回合把答复拆成三块、间隔 100 ms（`读` / `完了` / `。`）。

| 屏幕上第一次出现的字 | 修复前（`on_stream` 空实现） | 修复后 |
|---|---|---|
| `我先看看`（工具调用前的半句） | **从未出现** | **0.01 s** |
| `完了` | 0.62 s | 0.42 s |
| `读完了。` | 0.62 s | 0.52 s |
| **两块之间的间隔** | **0.000 s** | **0.102 s** |

- 修复前 `完了` 和 `读完了。` 的时间戳**完全相同**——同一个重绘里出现的，这就是"按 turn 渲染"的直接证据；修复后间隔 0.102 s，与网关的 100 ms 分块一一对应。
- `我先看看` 更狠：那是工具调用前的半句，`ToolStart{first}` 会把会话的流式正文清掉，所以它**只存在于流式帧里**——修复前它根本没有任何机会上屏。
- 定稿那一下的验收：正式条目落地前后两帧的屏幕**逐行相同**，且 `读完了。` 在最终屏幕上**只出现一次**（不重不漏）。

单测四条（`src/tui/zone/main/history/mod.rs`、`src/tui/session/view.rs`）：尾巴贴底、尾巴不进缓存、Ctrl+T 管得到尾巴里的思考、定稿前后逐行一致、满屏时尾巴仍可见且往上滚会整段退出、增长不拽走未跟随的读者。

### 7.4 视野外的尾巴不该出图（用户的直觉，已实测并修）

用户的场景：AI 正在说，往上划走了，划的时候它有 4 行，读了一会儿划回来它
已经有 8 行——**不在视野里的时候，把中间那几帧按"在视野里"渲染一遍是白费**。

修法（`HistoryZone::render_rows`）：尾巴只在**可能上屏**时才量。

```rust
if self.chat_scroll < self.bottom_height().saturating_add(viewport) {
    self.live_rows.materialize(&self.live, width, !self.reasoning_folded, t);
}
```

这个判据为什么是安全的，值得写下来：流式正文**只增不减**（模型只在末尾追加），
所以「上一次量出的高度」永远是**下界**；而偏移的补偿又保证了「尾巴的顶相对视口
的位置」不随增长变化——两条合起来，判据为假时尾巴**永远**进不了视野，不会漏画。

跳过量的时候偏移也**不再补偿**（画布底部没动，位移为零），画面就此冻住；
等它重新滚进「可能上屏」区、真的量出高度时，那次高度跳跃**一次性**补进偏移，
所以滚回来内容只按滚动量平移，不跳。

实测（`tui_scale_bench stream <转录条数> <回复字符数>`，120x40，600 帧）：

| 场景 | 无尾巴 | 尾巴在视野外 | 尾巴在视野内 |
|---|---|---|---|
| 8 000 条 / 500 字回复 | 2.89 ms | **2.75 ms（−0.14）** | 3.76 ms（+0.87） |
| 8 000 条 / 4 000 字 | 2.83 ms | **2.79 ms（−0.04）** | 3.76 ms（+0.94） |
| 8 000 条 / 16 000 字 | 2.79 ms | **2.68 ms（−0.11）** | 4.07 ms（+1.28） |
| 32 000 条 / 4 000 字 | 2.72 ms | **2.75 ms（+0.03）** | 3.71 ms（+0.99） |

**修之前**，视野外的尾巴是 **+1.31 ms/帧**——正是用户凭直觉指出的那笔浪费。
修之后是 0（噪声内）。视野内那 +0.9–1.3 ms 是必须花的：字要在屏幕上长出来。
注意视野内的代价随回复长度增长（500 字 0.87 ms → 16 000 字 1.28 ms），但
**与转录规模无关**——尾巴的代价只跟它自己那段文本有关。

**故意没做的事**：没有做「只增量折行尾部新增的那几行」。整段重折一次是
~1 ms，而增量折行要跟踪 markdown 块边界（未闭合围栏会让已经折过的行重新
排版），换不来这 1 ms。

**还没解决的那一层**（更大）：AI 说话时 daemon 每 ≤33 ms 推一帧，主循环
「每批信号画一帧」——所以哪怕尾巴在视野外、屏幕**一个像素都不变**，那 2.7 ms
的整帧渲染照样每 33 ms 跑一次（约 8% 单核），只是写出去的字节数≈0。要根治得
让子区能上报「这一帧我没变」，主循环据此跳过绘制。这是框架级的改动，没做。

### 7.6 用户消息要等回合结束才上屏（服务端记账 bug，已修）

用户报的现象：**按回车后屏幕上什么都不动**，用户消息不是立刻出现，而是和模型的
回复一起回来。「这不合理吧？」——不合理，是个真 bug。

根因在服务端的尾巴记账上：

```rust
// Session::start_turn —— 它把用户那条消息写进转录……
self.transcript.push(e.clone());
self.pending.push(e);
// ……报的却是这个：
Change::Stream          // ← 不是 Change::Transcript
```

而 `finish_submit` 连这个返回值都扔了（`self.state.start_turn(text);` 没接）。
daemon 的 pump 只在**事件**说 `Transcript/ToolActivity/TurnDone/Session` 时才发尾巴，
所以提交之后没有任何东西提醒它「尾巴长了」：用户那条消息要等到本回合第一个
**改变转录**的事件才搭车发出——对纯文本回复来说，那个事件就是 `TurnDone`，
也就是回合结束。于是「回车 → 屏幕上不动 → 回复整段冒出来」正是它的表现。

修法：**尾巴是派生状态，不靠事件提醒**。pump 在事件那一轮之后再对每个 watcher
的账本对一遍（`transcript_msgs_for` 没变化时直接返回空，刚发过的不会重复）：

```rust
for id in self.watchers.keys().copied().collect::<Vec<_>>() {
    let conns = self.watchers.get(&id).cloned().unwrap_or_default();
    for cid in conns {
        for m in self.transcript_msgs_for(id, cid) { self.enqueue(cid, m); }
    }
}
```

只补尾巴、不补状态：前端看得见的顺序是「转录先于描述它的那条状态」（收到
`busy=false` 就意味着这一轮结束了，此时它的转录里必须已经有一轮的条目）。

**证据**（回归测试 + 真实进程）：

- 新测试 `the_submitters_own_message_ships_before_the_round_does_anything`
  （`daemon/tests.rs`）：断言「第一条带转录内容的消息里只有用户那条，没有回复/
  工具」。**修之前它失败**，报的正是用户看到的那一幕：

  ```
  用户消息搭了回合事件的便车：\[User { content: "问" },
    Assistant { content: "你好", ... }\]
  ```

- 真实进程（pty + 伪网关，网关故意**延迟 2 秒**才吐第一个字）：

  | 屏幕上第一次出现 | 修复前 | 修复后 |
  |---|---|---|
  | `我要测试Markdown`（用户自己那条） | **始终没出现**（10 秒窗口内） | **0.01 s** |
  | `Markdown 基本语法演示`（回复首行） | — | 2.01 s（= 网关的 2 秒延迟） |

  修复前那条消息不是「晚一点」——这一轮里它**根本没上过屏**：回合结束时它才到，
  而那时 27 行的视口已经被 28 行的回复占满，用户卡直接被顶出视野。

### 7.7 流式尾巴让滚动切片错位（我引入的，已修 + 属性测试）

尾巴贴底时，画布 = 转录 + 尾巴，`chat_scroll` 要按「离画布底部」换算成「离转录
底部」才能交给块缓存。我为了省掉视野外的折行加了跳过判据：

```rust
if self.chat_scroll < self.bottom_height().saturating_add(viewport) { ... }
```

`bottom_height()` 在**还没量过**时是 0——而那 0 不是「下界」，是「不知道」。
按 0 跳过，画布就会假装没有尾巴，换算整体差一个尾巴的高度：**往上翻时内容
整块错位**，带底色的块（用户消息卡）会出现在不属于它的行上。

修法：没量过就先量一次（`!self.live_rows.measured()`）。

**证据**：新增属性测试 `offsets_slice_the_canvas_exactly_with_a_live_tail`——
「偏移 N 的视口 == 合成画布去掉最后 N 行后的末尾 viewport 行」，视口 6/12/25 ×
偏移 0/1/3/7/20/total−viewport，外加「量过之后跳过渲染」那一帧。**修之前它失败**：

```
viewport=6 offset=7（start=71）切片不对
  left:  ["", "+- \[ok\] search: ...", ...]      ← 画的是转录底部（错位）
  right: ["", "已切换到模型 X", "", "收尾一句。", "", "流式第一行"]   ← 该画的
```

---

### 7.8 非流式接线：`buffered` 真的生效了，而且是**一块一块**不是**一个 turn 一个 turn**

`set_stream_mode` 从第四轮起就是"存在但没有任何调用者"，`config.yaml → app.streaming`
与 `MYPI_STREAM_MODE` 解析得出来、写进配置、然后**没有读者**。这一轮把它接上，并且
把"非流式"的语义定成用户要的那个：

| 环节 | 做法 |
|---|---|
| 来源 | `main.rs::session_spec`：`stream_mode: cfg.app.streaming`（`MYPI_STREAM_MODE` 覆盖在 config 解析那一层） |
| 传下去 | `SessionSpec` 新增 `stream_mode` 字段；`hub::assemble` 建完会话调一次 `set_stream_mode`（`open_new` 与 `resume` 走同一条路，所以 resume 吃的是**当下**的配置） |
| 语义 | `Buffered` = **每一块**的文本在"这一块的文本写完"时整份交出：模型要调工具时（那一轮的文本到此为止）、以及回合结束时。**不是**攒到回合末尾一次性倒出来 |

交付点必须在工具边界上，有三个理由，第三个是真 bug：

1. 「一块一块」而不是「一个 turn 一个 turn」；
2. 那半句随后会作为工具条目的一部分存档（`Entry::ToolRequest.text`），交在这里正好衔接；
3. **憋到最后再灌一遍会重复**：会话在 `ToolStart` 时已经把流式槽清空了（那半句归工具条目了），
   回合末再灌一次，工具调用前那半句就被**粘进最终回复**里——回复和工具卡各显示一遍。

**验证**（真实进程 + pty + 伪网关：先调一次工具，再把答复拆成三块、每块 100 ms）：

| 屏幕上第一次出现 | `immediate` | `buffered` |
|---|---|---|
| `read`（工具卡） | 0.31 s | **0.31 s**（自己那块，没等到回合末） |
| `完了` | 0.42 s | 0.62 s |
| `读完了。` | 0.52 s | 0.62 s |
| **两块之间的间隔** | **0.103 s**（逐字） | **0.000 s**（整份到达） |

`buffered` 那行就是"非流式"该有的样子：工具卡按自己那块的时间到（不是跟回复一起到），
答复作为**一整块**出现。配置文件那条路单跑了一遍（`config.yaml` 写 `streaming: buffered`、
不设环境变量）结果一致。

单测三条：`buffered_hands_over_each_block_not_the_whole_turn`（第一轮那半句的交出
位置在工具调用**之前**，且每轮一份）、`buffered_keeps_the_tool_rounds_text_out_of_the_final_reply`
（事件流灌进会话后最终回复只含最后一轮文本）、`the_stream_mode_rides_the_spec_into_the_session`
（spec → 会话，含 resume 路径）；配置侧 `the_stream_mode_comes_from_config_yaml_too`（含拼错必须报错）。

**顺带发现一个显示缺口**（没改，等你定）：模型在工具调用前那半句（`Entry::ToolRequest.text`）
**渲染侧没有任何地方画它**——`single_node` 的 `ToolRequest` 分支只把 `args` 交给 `tool_card`，
`text` 被 `..` 吃掉了。于是：

- `immediate`：你只在流式尾巴上**瞥见**它（工具卡一来，会话清槽，它就没了）；
- `buffered`：它**完全不上屏**（那一块的文本交付了，但没人画）。

它不是路的问题（wire 上一直有、库里也有），是画的问题。要画的话有两种：作为正文
放在工具卡**上面**（它本来就是助手这一段的话），或者作为卡内的一行。**我没动**——
它牵涉工具卡的版式，而工具表规范这一轮就要做，一起定更顺。

---

### 7.5 顺带把「256 块 = 18 MB」这个读法推翻了

用户问：预算改小能不能降内存？实测（同一套 live-tail 代码，只改 `BLOCK_BUDGET`）：

| 规模 | full 均值 256 / 32 | mixed 均值 256 / 32 | 峰值 RSS 256 / 32 |
|---|---|---|---|
| 8 000 | 3.33 / 3.83 ms | 3.00 / 2.99 ms | 41.0 / **37.1 MB** |
| 12 000 | 3.92 / 3.80 ms | 2.97 / 3.01 ms | 46.6 / **42.5 MB** |
| 16 000 | 3.97 / 3.88 ms | 3.00 / 3.12 ms | 52.4 / **47.5 MB** |
| 24 000 | 4.07 / 3.88 ms | 3.10 / 3.01 ms | 62.6 / **58.1 MB** |
| 32 000 | 4.19 / 3.96 ms | 3.35 / 3.10 ms | 73.0 / **68.7 MB** |

- **省下的是 ~4 MB**，不是「18 MB 的缓存 → 2 MB」。同一配置两次运行 8k 的 full 均值为 4.03 与 3.33 ms，**运行间抖动 ±20%**，所以上表里 32 那边"稍快"不能当结论——能说的只有**没有变慢**。
- 内存为什么只降 4 MB：满缓存那 9k–11k 行本身只有 **2–3 MB**（一行 `Line` 加几个 `Span` 而已）。§6.1 量的「首帧 → 满缓存 18 MB」主要是**遍历时渲染那五千到两万个块留下的分配器高水位**——画的当下申请、之后释放，页仍留在进程里。缓存砍到八分之一，这部分照样发生。
- 结论：`BLOCK_BUDGET` 是**回溯深度**旋钮，不是内存旋钮。32 块 ≈ 1 200–1 800 行 ≈ 30–45 屏；256 块 ≈ 9k–11k 行 ≈ 230 屏。两个方向都不该拿它去修 MB 数。注释已按此改写。

---

## 8. 问题清单

### 已修（四轮合计）

| # | 问题 | 证据 |
|---|---|---|
| 1 | `load_path` 外层 join O(n²) | 2k/4k/8k = 2.9 s / 12.7 s / 52.9 s → 10.6 / 20.6 / 39.6 ms，结果逐行一致 |
| 2 | `effective_name` 每次 resume 重跑同一个平方查询 | 8k 条 resume 约 106 s → 约 40 ms |
| 3 | `dispatch` 对 `Transcript` 无谓克隆（外加 `Stream`/`State` 的逐字段克隆） | 32k 条 attach 常驻 83.3 → 45.9 MB；克隆本身 17.3 ms |
| 4 | 握手 15 s 读超时把慢服务端变成进程死亡 | 超时改报 `daemon 在 N 秒内没有回应`；3 个新测试，其中一个修复前必然失败 |
| 5 | 客户端逐行扫描 O(n²) | 一条 39 MB 的消息要扫约 46 GB；16k 转录上屏 3.46 s → 0.16 s |
| 6 | `MAX_LINE_BYTES` 32 MB 装不下 32k 条的快照 | 32k 条**永远画不出内容** → 0.29 s（快照分块发出） |
| 7 | 解码失败被报成"连接已断开" | 按错误类型分派文案 |
| 8 | 块缓存注释与实测不符 | 注释按实测改写；第四轮进一步证明「18 MB」是**分配器高水位**而非缓存驻留（256 → 32 块只省 ~4 MB，§7.5） |
| 9 | 走查对经过的每一块都重新渲染 | 每帧代价 = 2.5 µs × 离底部的行数；8k 滚到 36000 行时 94 ms/帧 → 修复后 4.0 ms 且与偏移无关 |
| 10 | 用户消息要等回合结束才上屏 | 服务端按事件发尾巴，而 `start_turn` 报的是 `Change::Stream`、`finish_submit` 又把它扔了；修前后：用户那条 **从未上屏 → 0.01 s**。见 §7.6 |
| 11 | 流式尾巴使滚动切片错位（尾巴没量过时按高度 0 换算） | 属性测试 `offsets_slice_the_canvas_exactly_with_a_live_tail` 修前失败、修后通过。见 §7.7 |
| 12 | `on_stream` 空实现 → 按 turn 渲染 | 真实 pty：修复前三块答复同一时刻上屏（间隔 0.000 s）→ 修复后 0.102 s；工具调用前那半句修复前从未上屏。定稿前后屏幕逐行相同 |
| 13 | `Session::set_stream_mode` 没有调用者 | 已接：`SessionSpec.stream_mode` ← `config.yaml → app.streaming` / `MYPI_STREAM_MODE`；`hub::assemble` 建会话后调一次。见 §7.8 |

### 未修（按优先级）

| # | 问题 | 证据 | 影响 | 建议 |
|---|---|---|---|---|
| 14 | 服务端 `transcript_msgs_for` 每 watcher 各切一份块 | `daemon/mod.rs` | 多前端时线性放大 | 分块结果可跨 watcher 共享（同一 generation 的切片是同一份） |
| 15 | 服务端 attach 内存仍约为载荷 3.7× | 32k 条 daemon 峰值 132.8 MB | 单会话峰值 130 MB | 条目 + `ChatContext` 两份叠加；上下文可延迟重建 |
| 16 | 流式期间每 ≤33 ms 整帧重绘，屏幕无变化也照画 | `stream` 档：2.7 ms/帧 × 30/s ≈ 8% 单核，ANSI 写出≈0 | 后台常驻开销 | 让子区上报「无变化」，主循环跳过 draw（框架级） |
| 17 | daemon 与 TUI 各持一份完整转录 | 32k 条合计约 196 MB 常驻 | 结构性双份成本 | 短期无解，记着 |
| 18 | 60 fps 下 0–1 帧冷缓存尖峰 9–37 ms | 归因在滚轮帧（滚进未缓存区段） | 一次性，不影响手感 | 观察 |
| 19 | 分块粒度按条数，不按字节 | 2000 条/块；单条极大时仍可能触顶 | 巨物机制下不成问题 | 观察 |
| 20 | daemon 的 banner 漏进 TUI 终端 | spawn 只把 stdin 接 null，stdout 继承 | 首帧前闪一行 `mypi daemon listening on …` | spawn 时 stdout 也接 null |
| 21 | Ctrl-T/Ctrl-O 单帧 46–65 ms | 全局变体失效迫使视口内所有块重画 | 一次性顿挫，0.7–0.8% 的帧 | 可只失效视口内的变体；需要设计 |

---

## 9. 附录 A：TUI 侧 app / zone / subzone / components 现状整理

### A.1 层级与职责

```
crossterm Event ──┐
                  ├─→ Signal ──→ 主循环 ──→ App::deliver(RawEvent) ──→ MainZone::deliver
daemon ServerMsg ─┘                                                    │
                                                                       ├─→ HistoryZone
                                                                       ├─→ InputZone
                                                                       └─→ ReservedZone
                                                                              └─→ StatusLine ──→ 7 个 StatusComponent
```

| 层 | 文件 | 职责 | 不做什么 |
|---|---|---|---|
| APP (Router) | `src/tui/app.rs` | 接入事件、归一化、下发所有权、登记交接、启动传尺寸 | 不渲染、不仲裁行高、不碰 Zone 内部 |
| Zone | `src/tui/zone/main/mod.rs` | 行高仲裁、广播、动作转发 | 不实现任何子区语义 |
| SubZone | `history/`、`input/`、`reserved/` | 申请行高、声明接受哪些物理事件、自渲染 | 不跨子区调用 |
| Component | `input/statusline/components/` | 消费 `StatusEvent`、自己决定显示/隐藏/着色 | 不知道其他组件存在 |

### A.2 三个子区的契约差异

| 子区 | 行高策略 | 声明的事件 | 私有状态 |
|---|---|---|---|
| history | 申请 `u16::MAX`（吃剩余） | Key、ScrollUp、ScrollDown | 滚动位置、折叠开关、转录、块缓存、代 |
| input | 自限（按换行算，上限 1/4 高） | Key、Paste | 编辑器、历史、hang 状态、出口请求 |
| reserved | 按持有者声明 | Key | 补全服务、借出/回执 |

**行高仲裁规则**（`MainZone::arbitrate`）：输入区先申请 → 保留区按声明 → 历史区吃剩余。`settle()` 每帧结算，**变了才广播**（广播语义是「可用行高变了」）。

### A.3 状态栏组件（7 个）

| 组件 | side | order | priority | 说明 |
|---|---|---|---|---|
| activity | Left | — | BASE(100) | π 前缀，永不裁 |
| cost | Left | — | BASE(100) | 价格，永不裁 |
| usage | Flex | 0 | BASE(100) | 量表，永不裁 |
| cwd | Left | — | 40 | `[D] 目录名` |
| git | Left | 30 | 55 | 两档：`@ branch ?N +N` / 空间不足只留 `@ branch` |
| session | Left | — | 60 | `< 会话名`，空则隐藏 |
| model | Left | — | 80 | `[M] 显示名` |

**裁剪引擎**（`statusline/layout.rs`）：循环淘汰优先级最低的组件直到行宽装得下；「宽度越大越早被裁」是淘汰顺序的自然结果，不是额外规则。BASE 层永不参与淘汰。

### A.4 事件形状的现状

| 事件类型 | 定义处 | 消费者 |
|---|---|---|
| `RawEvent`（Key/Paste/ScrollUp/ScrollDown） | `tui/keys.rs` | SubZone 按 `accepts()` 声明 |
| `StatusEvent`（Tick/ActivityStarted/Stopped/ModelChanged/WorkspaceChanged/CwdMigrated/SessionRenamed/Git/Usage） | `input/statusline/mod.rs` | 7 个组件广播式消费 |
| `Signal`（Key/Paste/Mouse/Resized/Server/Git） | `tui/session/signal.rs` | 主循环 |
| `ServerMsg`（12 个变体） | `server/wire.rs` | `view::dispatch` → `SessionView` |
| `SessionEvent`（12 个变体，服务端内部） | `server/events.rs` | hub → daemon pump → wire |
| `Change`（6 个变体，粗粒度脏标记） | `server/events.rs` | daemon pump 合并推送 |

**设计上的亮点**（值得保持）：`Change` 把「发生了什么」与「要重画什么」分开，`pump` 合并连续 `Stream` 变化为一次快照；`StatusEvent` 是纯广播，组件自己决定消费与否，Zone 不做路由。

---

## 10. 附录 B：事件广播与响应的规范化评估

### B.1 现状中的不一致

| # | 现象 | 位置 | 问题 |
|---|---|---|---|
| 1 | 三个子区各自解释原始键（Ctrl+T/Ctrl+O 在 history，其余在 input） | `history/mod.rs::interpret` | 键位语义分散；无冲突检测。今天靠「不同键位」避免，明天加一个键就可能两个子区都响应 |
| 2 | `accepts()` 声明到「类别」粒度，不到键位 | `zone/mod.rs` | 声明说「我要 Key」，实际只解释其中几个；Zone 无法预判，只能全发 |
| 3 | `SubZone::deliver` 返回 `bool` 但 Zone 忽略返回值 | `main/mod.rs::deliver` | 契约上有「消费」概念，实现上是纯广播，返回值是死代码 |
| 4 | 出口请求用 `Option<ExitRequest>` 逐级 `take()` | `input` → `main` → `app` | 手写穿透链，多一个出口要改三处 |
| 5 | `StatusEvent` 有 9 个变体但状态栏组件只关心其中 6 个 | `statusline/mod.rs` | 每次广播都是 7 次 match，代价可忽略但语义模糊 |
| 6 | `ServerMsg` → `SessionView` 的 6 个方法里，`on_stream` 是空的 | `view.rs` | 翻译层声明了流式能力，实例未兑现 |

### B.2 建议

**（a）事件分类建议分三层，显式命名**：

```
物理层   RawEvent      —— 只描述「终端说了什么」，零语义（现状已是）
语义层   Action        —— 「用户想干什么」（现有 semantics::Action，但目前只在输入区内部用）
状态层   StatusEvent / ServerMsg —— 「世界变成什么样了」
```

现状缺的是**语义层的统一**：`semantics::Action` 只覆盖编辑器动作，Ctrl+T/Ctrl+O 这类「视图语义动作」在 history 子区里被直接解释成布尔翻转。建议把折叠开关提升为语义动作（`Action::ToggleReasoning` / `ToggleTools`），由 `keys` 层映射，history 只接收动作。收益：键位表集中一处，可测、可重绑、可发现冲突。

**（b）`accepts()` 升级为「声明 + 消费确认」**：让 `deliver` 的 `bool` 真正参与仲裁——第一个声明消费的子区拿走事件，后续子区不再收到。必要性中等，收益是消除「两个子区同时响应同一键」的隐患。

**（c）出口请求改为事件队列**：`InputZone` 往一个 `Vec<ExitRequest>` 里 push，Zone/APP 只负责搬运，不再逐级 `Option::take`。必要性高（现在每加一个出口动作要动三层），收益直接。

**（d）不要做的事**：不要引入异步/回调式事件总线。当前「主循环阻塞收信号 → 合并批次 → 一次性下发 → 一帧重绘」的同步模型是性能优秀（3 ms/帧）的直接原因，改成发布订阅只会增加延迟与不确定性。

---

## 11. 附录 C：工具注册规范的重设计

### C.1 现状：一个工具的知识分散在 5 处

以 `bash` 为例，要改一个工具必须同时改：

| # | 位置 | 内容 |
|---|---|---|
| 1 | `tools.rs::definitions()` | JSON schema + 中文描述（给模型看的） |
| 2 | `tools.rs::BuiltinTools::execute` 的 `match` | 名字 → 函数 |
| 3 | 各自的 `parse_*_args` | 参数反序列化 + 校验 |
| 4 | `BuiltinTools::with_enabled` / `allowed()` | profile 过滤（`context` 硬编码例外，出现两处） |
| 5 | `#[cfg(feature = "web")]` | 编译期存在性，且要在 `execute` 里再写一遍 `not(feature)` 分支来「诚实地拒绝幻觉调用」 |

共 9 个工具（read/edit/cd/bash/mass_edit/fetch/browser/search/context），上述 5 处都要与它保持一致。

**风险**：加工具时漏改任何一处，编译器只在第 2 处（match 穷尽性不存在，是 `other =>` 兜底）和第 5 处报错——漏改 1 会让工具对模型可见但调用即失败；漏改 4 会让 profile 过滤静默失效。

### C.2 建议：单表注册 + 派生一切

```rust
pub struct Tool {
    pub name: &'static str,
    pub description: &'static str,
    pub schema: fn() -> serde_json::Value,
    /// 从原始 JSON 参数串解析并执行。唯一的行为入口。
    pub run: fn(&mut ToolCtx, &str) -> Result<String>,
    /// 编译期可用性：false 时既不出现在 definitions，也拒绝执行。
    pub available: fn() -> bool,
    /// 是否可被 profile 过滤（context 为 false）。
    pub filterable: bool,
}

pub const TOOLS: &[Tool] = &[ /* 9 项 */ ];
```

四个派生点全部由 `TOOLS` 生成：`definitions()`、`definitions_for(enabled)`、`execute` 的查找、`allowed()`。收益：

- 加工具只改一个地方（新增一个 `Tool` 字面量），**编译器保证四处一致**。
- `#[cfg(feature = "web")]` 收敛为 `available: || cfg!(feature = "web")` 一个表达式，删掉 `execute` 里的 `not(feature)` 重复分支。
- `context` 的「不受过滤」例外从 2 处硬编码变成 1 个字段。
- 工具的自测（参数解析的边界用例）可以挂到 `Tool` 上，注册即被测。

**必要性：高。收益：高（消除 5 处一致性负担，且这是最容易加错的地方）。** 代价：一次性重排 `tools.rs` 约 1000 行，机械但量大。

### C.3 顺带建议

- 工具名与 profile 名单目前是字符串比较（`set.contains(&d.function.name)`）。建议引入 `ToolName` 枚举 + `FromStr`，profile 配置在解析时就校验未知名字（今天写错一个名字只是静默过滤掉）。
- `definitions()` 每次都构造 `Vec<ToolDef>`（含 JSON 字面量）。它是每轮请求调用一次，不在热路径上，暂不必优化——但如果 `TOOLS` 改成 `const`，可以顺手用 `OnceLock` 缓存。

---

## 12. 附录 D：斜杠命令调用规范的重设计

### D.1 现状：命令表存在，但**没有任何分发**

`src/tui/zone/main/reserved/completion/engine.rs` 里有 `COMMANDS`：10 个 `CommandSpec`（`/model`、`/switch`、`/name`、`/cdp`、`/resume`、`/compact`、`/profile`、`/q`、`/quit`、`/exit`），带描述和 `ArgKind`。

但全仓库搜索 `engine::lookup` 只有两处调用，**都在补全控制器里**（`completion/controller.rs:95`、`:232`）。也就是说：

- `COMMANDS` 只驱动**补全候选**。
- 回车提交 `/name foo` 时，输入区走的是普通提交路径（`input/mod.rs:195` `ExitRequest::Submit(text)`），整串文本被当作**用户消息**发给 daemon，进模型上下文。
- 代码注释写着「Dispatch (`App::submit`), completion candidates, and argument shape all read from this table」——这句是**过时的**：`App::submit` 已经不存在（会话状态搬到服务端后），分发没有重建。

这解释了已知现状「`/name` 没做好」，而且比预期更严重：**所有 10 个斜杠命令目前都不生效**，只会变成发给模型的聊天内容。

### D.2 建议：命令走 wire，不走文本

斜杠命令天然是**会话操作**（改名、切模型、切目录、压缩、恢复），而会话状态在服务端。所以分发点应在服务端，前端只做「识别 + 转译」：

```
输入区：文本以 '/' 开头且第一个词能 lookup 到
        → 不发 Submit{text}，发 Command{name, args}
daemon：ClientMsg::Command { name: String, args: String }
        → 查服务端命令表 → 执行 → 结果以 Entry::System 通知回灌转录
```

理由：

1. **命令的语义属于会话**，而会话在服务端。前端本地执行 `/cd` 之类会与「服务端持有 cwd 真相」冲突（`Session::set_cwd`、`cwd_history` 都在服务端）。
2. 命令结果需要出现在转录里（「已切换到模型 X」），服务端本来就是转录的唯一写入者。
3. 未来 web/telegram 前端自动获得全部命令，不需要各自实现一遍。
4. 前端只保留**纯本地**命令（`/q` 退出 TUI），用一个 `LOCAL_COMMANDS` 小集合区分。

配套规范化：

- 命令表**一份**（放服务端，通过 wire 的 `HelloOk` 或新消息下发给前端做补全），删掉前端 `COMMANDS` 的重复定义。今天前端表里已有 `/model` 写 config.yaml 这类「前端才做得到」的命令，正好借这次把「本地命令」与「会话命令」分开列。
- `ArgKind`（`None`/`ModelId`/`Path`）目前只影响补全，应同时用于**参数校验**，在服务端拒绝非法参数并回一条 `Entry::System` 说明。
- 提交路径上加一道「命令识别」，未识别的 `/xxx` 应提示「未知命令」而不是静默发给模型——今天 `/notacommand` 会进模型上下文，浪费 token 且模型会困惑。

**必要性：高。收益：高。** 这是「命令完全不工作」的修复，同时把命令表从两处收敛到一处。

---

## 13. 附录 E：可进一步规范化的点（按收益/必要性排序）

| # | 点 | 位置 | 必要性 | 收益 | 代价 |
|---|---|---|---|---|---|
| 1 | ~~`load_path` O(n²)~~ | `store.rs` | **已做** | 长会话 attach 从不可用变可用（1336×） | 已完成 |
| 1b | ~~`dispatch` 的 `Transcript` 克隆~~ | `view.rs` | **已做** | 32k 条 attach 常驻 −37 MB | 已完成 |
| 2 | ~~握手超时与读超时解耦~~ | `wire.rs` | **已做** | 慢服务端不再表现为前端崩溃；超时可读 | 已完成 |
| 3 | 斜杠命令分发（走 wire） | 见附录 D | 高 | 10 个命令从全部失效变可用；命令表收敛到一处 | 中 |
| 4 | 工具单表注册 | 见附录 C | 高 | 消除 5 处一致性负担 | 大（机械） |
| 5 | 语义动作层统一（折叠开关等） | `keys.rs` / `history` | 中 | 键位可发现、可重绑、可测 | 中 |
| 6 | 出口请求改事件队列 | `input` → `main` → `app` | 中 | 加出口动作不再改三层 | 小 |
| 7 | `dispatch` 去掉 `Transcript` clone | `view.rs:69` | 中 | 32k 条省 40 MB 瞬时峰值 | 小 |
| 8 | 工具名用枚举 + profile 名单解析期校验 | `tools.rs` / `profile.rs` | 中 | 配置写错名字立刻报错，而不是静默过滤 | 小 |
| 9 | `SubZone::deliver` 返回值参与仲裁 | `main/mod.rs` | 低 | 消除同键双响隐患 | 小 |
| 10 | 块缓存预算按巨物时代的块重估 | `history/cache/mod.rs` | 低 | **不再是内存问题**（§7.5：256→32 只省 4 MB） | 纯粹是回溯深度取舍：256 块 ≈ 230 屏，32 块 ≈ 30–45 屏且耗时无回归。等巨物机制落地后按当时块高重估 |

**不建议做的**：

- 不要为了「事件规范化」引入异步事件总线或回调注册。当前同步批处理模型是 3 ms/帧的原因。
- 不要给状态栏加「组件注册运行时 API」。7 个组件是编译期常量，`standard()` 里的静态数组就是最好的规范。
- 不要在 TUI 侧复制服务端已经持有的真相（cwd、模型元数据、会话名）。本次测试前刚把这几项归位到正确的一侧，别再搬回去。

---

## 14. 附录 F：对齐 omp —— 工具渲染、事件、命令到底该住在哪一侧

（源码：`/tmp/oh-my-pi`，已 clone 的最新版。行号即证据。）

### F.1 omp 的三层，中间是**数据**不是组件

| 层 | 位置 | 内容 |
|---|---|---|
| 工具（服务侧） | `packages/coding-agent/src/tools/read.ts` 等 | 执行 + 产出 `{content: 给模型的文本, details: 给 UI 的结构化数据}`。`details` 对核心是 **`unknown`**（`packages/agent/src/types.ts:952` `AgentToolResult.details?: T`） |
| 传输 / 持久化 | `packages/wire/src/index.ts:96,131` | `ToolResultMessage.details?: unknown` 走线**也**进 session entries；跨进程前端（ACP）同样带（`modes/acp/acp-event-mapper.ts:42`、`acp-agent.ts:1067`），回放时前端**从 details 重建渲染所需的参数**（`acp-agent.ts:2456 #buildReplayToolArgs`） |
| 渲染（UI 侧） | `packages/tui/src/tools/` | `index.ts` 一张 `name → ToolRenderer` 注册表 + `renderDefaultToolExecution` 兜底；`renderer.ts` 定义接口：`renderCall(args, options, theme)` / `renderResult(result, options, theme, args?)` 返回 `Component \| undefined`，外加展示策略位：`mergeCallAndResult`、`inline`、`animatedPendingPreview`、`forceFirstResultViewportRepaint`、`activitySummary` |

**所以「服务侧做接口、TUI 去调用」的准确形态是：**

- 服务侧给的**不是组件**，是三样数据：`details` 载荷、工具声明的元数据（`label`/`summary`/`approval` tier/`loadMode`/`intent` 策略）、事件（`tool_execution_start` / `_update` / `_end`）。
- 组件（`Component`）是 UI 概念，服务侧给不出来：omp 里 renderer 明确住在 tui 包，一个 web UI 会把同一份 `details` 画成 HTML。
- **换 UI 不用重写工具**的机制 = 换 renderer 表（或只换组件层），工具与 `details` 一字不动。

一个反直觉但关键的点：omp 的 `*Details` 类型**声明在 UI 包**（`packages/tui/src/tools/read.ts:30` 导出 `ReadToolDetails`），工具实现**反向 import** 它（`coding-agent/src/tools/read.ts:1`）。即：**契约声明在消费方，服务侧只是产出者**。我们进程隔离（daemon 与 TUI 是两个进程），连 import 都不可能，所以只能走 JSON —— 而我们的 wire 已经在了，`details` 挂上去即可。

### F.2 我们仓库的对应关系

| omp | 我们 | 动作 |
|---|---|---|
| `AgentToolResult.details` | `Entry::ToolResult{call_id,name,ok,result}` 只有文本 | 加 `details: serde_json::Value`（`#[serde(default)]`，向后兼容，不必动 `PROTO_VERSION`）；DB payload 同步 |
| `ToolView::synthesize`（从文本反推视图） | 我们**正在用**这个反模式：diff 是从 `- `/`+ ` 前缀猜的，还住在 `server/entry.rs`（数据模型层） | 由工具产出 `details`，渲染侧读 `details`，`synthesize` 退场 |
| `tui/src/tools/index.ts` 注册表 + 兜底 | `tui/.../history/render/tools.rs` 里 **9 处 `match name`** | 收敛成 `ToolRenderer` trait + `name → renderer` 表 + `default_renderer`，渲染器一工具一文件 |
| `renderCall`/`renderResult` 的 `options{expanded, isPartial, spinnerFrame}` | 我们只有 `expanded` | 加 `is_partial`/`spinner_frame`（进度要用） |
| 共享原语 `plainToolCard`/`renderStatusLine`/`formatOutputPaneLines`/`result-card.ts` | 我们有 `cards.rs::card_edge/card_row/on_bg/pad_to` | 结构对得上，不用改 |
| 折叠/预览阈值（`DEFAULT_MAX_LINES=3000`/`DEFAULT_MAX_BYTES=50KiB`，`tui/src/tools/streaming-output.ts:10,12`） | 我们有 `DEFAULT_FOLD/DIFF_FOLD/WEB_FOLD` | 数值取 omp 的，且改成**可配置**（用户要求） |

### F.3 斜杠命令：omp 也放在服务侧，而且已经分好层

`packages/coding-agent/src/slash-commands/`（4680 行，**在 coding-agent 包，不在 tui**）：

- `types.ts`：`BuiltinSlashCommand` = **纯元数据**（`name`/`aliases`/`description`/`icon`/`allowArgs`/`subcommands`/`inlineHint`）；`SlashCommandSpec extends BuiltinSlashCommand` = 元数据 + handler。
- handler 返回 `SlashCommandResult = undefined | { consumed: true; agentInvoked? } | { prompt: string }` —— **命令要么自己消化，要么产出一条 prompt 变成用户消息**。
- 元数据单独导出（`BUILTIN_SLASH_COMMAND_DEFS`，`builtin-registry.ts`），注释写明用途：「autocomplete and help text」，且被 TUI 与 ACP **两个前端共用**（"Unified slash-command spec consumed by both TUI and ACP dispatchers"）。
- 唯一的 TUI 专属漏点：`getArgumentCompletions?: (prefix) => AutocompleteItem[]`（参数补全返回 UI 控件类型）。元数据本身干净。

这印证了附录 D 的方向（命令走服务端），并且给出可抄的分法：**元数据 UI 无关 + handler 返回 `{consumed}` 或 `{prompt}`**。

### F.4 目录建议

不要 `util/ui`。按 omp 的分法：

- 契约（`details` 形状 + 工具元数据表）在 **服务侧**（`server/agent/tools.rs` 的 spec 表 + `server/entry.rs` 的 `details` 字段）；
- 渲染器在 **TUI 侧**：`tui/zone/main/history/render/tools/` 一工具一文件 + 一张注册表 + 兜底；
- 斜杠命令：元数据与 handler 在**服务侧**，TUI 只拿元数据做补全、把 `{prompt}`/wire 消息发出去。

### F.5 修正附录 C.1 的计数

C.1 说「一个工具的知识分散在 5 处」——按代码实际清点（含渲染侧）是 **9 处**：`definitions()`、`execute()` 分派、`definitions_for()` 的 `context` 例外、`render/tools.rs` 的 `fold_limit`/`fold_takes_head`/`meta()`/`call_body()`/`result_sections()`、`entry.rs::ToolView::synthesize()`。单表注册要一次收掉这 9 处。

---

## 15. 第七轮：工具层重做（单表注册 + details 契约 + 读写边界）

### 15.1 一句话

一个工具的知识原来散在 **9 处**（schema / 分派 / profile 例外 / 渲染层 6 个 match / 数据模型层），
现在收敛成 `tools.rs` 里的一张表 `TOOLS`，`definitions_for`、`definitions`、`execute` 三处全部**派生**自它；
工具除了给模型的文本，还交一份 **`details`**（结构化载荷）给前端，渲染层不再从文本反推形状。

### 15.2 `ToolSpec`（`src/server/agent/tools.rs`）

| 字段 | 作用 |
|---|---|
| `name` / `description` | 工具名（也是 profile 名单的键）与给模型的手册 |
| `schema` | 该工具自己的 JSON-Schema 对象；**`intent` 由 `IntentPolicy` 注入**，9 份重复的字段定义删掉了 |
| `run` | `fn(&mut BuiltinTools, &str) -> Result<ToolOutput>`，唯一的行为入口 |
| `available` | 编译期/运行期可用性（web 三件套按 feature 门控，且非 web 构建下的 run 体自带明确拒绝语） |
| `filterable` | 能否被 profile 关掉；`context` 为 `false`（压缩后唯一退路），原来硬编码两处 |
| `intent` | `Require` / `Optional` / `Omit` |
| `tier` | `Read` / `Write` / `Exec` —— 安全分类的声明位（谁能不能动盘，一眼可查） |
| `events` | **自定义事件**声明位；`start`/`end` 是循环对每次调用的保证，不在这里声明，工具也就无从漏掉 |

派生点：`definitions()` / `definitions_for()` / `execute()` 的查找 + 两道门。表自身的不变量有测试兜底
（名字唯一、schema 必有 `properties`/`required`、schema 里不许自己写 `intent`）。

### 15.3 `details` 契约（工具 → 前端）

`ToolOutput { text, details: Option<Value> }`（`loop_rs`）→ `ToolEvent::Finish.details` →
`SessionEvent::ToolFinish.details` → `Entry::ToolResult.details`（落库、上线、随 `Entry` 走 wire，老库 `#[serde(default)]` 兼容）。

- `text` 只给模型，也只有它进请求；
- `details` 只给前端，核心不认识（`Value`），**各 UI 自己解释**。这就是「换 UI 不用重写工具」的机制。
- 渲染侧 `ToolView` 从 `server/entry.rs` **搬到了渲染层**（UI 概念不该住在数据模型里），
  由 `ToolView::from_details` 读 `details`；`details` 缺席（工具没交，或老数据）退回纯文本。

**顺带发现并修掉的真窟窿**：`edit`/`mass_edit` 的 diff 渲染此前**根本到不了**——`synthesize` 是从结果文本里找 `- `/`+ ` 前缀，
而这两个工具返回的是 `replaced: <path>` 一行字，于是 `ToolView::Diff` 那条路只有测试桩在走。
现在 diff 由工具自己算好放进 `details`（它本来就精确知道换掉了哪几行），diff 渲染第一次真的活过来。

### 15.4 能力：边界、上限、分页、write

| 项 | 做法 |
|---|---|
| 工作区边界 | 新增 `workspace_root`（会话起始目录，**不随 cd 移动**）。read/edit/write/mass_edit 的目标必须在 **工作区 ∪ /tmp** 内——与 `bash_guard` 的许可区同一套概念。路径先做**符号链接感知的规范化**再判区，`/tmp/link/../../etc` 这类逃逸骗不过 |
| `cd` 受限 | 只能在工作区内迁移（越界拒绝并保持 cwd 不变）。否则「边界」会跟着被边界的东西一起跑 |
| read 上限 + 分页 | 默认取 `tools.outputMaxLines/outputMaxBytes`（缺省 512 行 / 256 KiB，即巨物阈值）。**读到上限就停**（不是读完再截），内存代价是上限而不是文件大小；末尾给 `[还有更多行；继续读用 offset=N]`。按字节截断且没扫到文件尾时**如实说总行数未知** |
| 巨物阈值可配 | `config.yaml → tools.outputMaxLines / outputMaxBytes`（camel/snake 都收），同一对数字供巨物溢出与 read 默认上限共用 |
| `write` 新工具 | 整份写入、同名**覆盖**；`details = {kind:"write", bytes, created}`；父目录不存在时拒绝并告诉它用 `mkdir -p` |

### 15.5 配置与 profile

- `browser.persistProfile`（默认 `true`）：关掉则每次进程起一个**一次性 profile**（`/tmp/mypi-browser-<pid>`，
  随浏览器进程一起删），登录态不跨会话；显式 `profileDir` / `MYPI_BROWSER_PROFILE_DIR` 仍然优先（点了路径就是要那个 profile 里的登录）。
  只影响**我们自己启动**的浏览器；attach 到别人在跑的浏览器时用的是人家的 profile。
- 工具启用的**对外接口**：`BuiltinTools::is_enabled(name)` / `enabled_tools()` —— 两道门（编译进来 + profile 允许）一次回答，
  状态栏、`mypi tools`、测试都不必自己再推一遍。
- 生效时机：名册在会话开始时读一次；`tools.reloadOnCompaction`（默认 `false`）允许在**压缩之后**这个合法缝隙再读一次。
  默认关闭的理由：中途冒出来的工具会让模型手里握着它已经看不见的工具的结果，并让 provider 前缀缓存作废。

### 15.6 验证

- 单测 **605 → 624**（新增：表不变量、intent 注入、名册与执行器同源、`is_enabled` 接口、edit/mass_edit 的 diff details、
  read 分页与字节上限的两条提示、越界读写被拒、write 新建/覆盖/缺父目录、`format_duration` 与 omp 逐档对齐、
  details 落库往返、cd 只许区内、压缩后名册重读的开关）；clippy `--all-targets` 无警告。
- **真实进程端到端**（伪网关驱动 `write` → 分页 `read` → 收尾，pty 回读屏幕，`/tmp/mypi-fake/tool_check.py`）：

| 屏幕上第一次出现 | 时刻 |
|---|---|
| `写一个测试文件`（write 卡的 header intent） | 0.02 s |
| `paged.txt`（卡的 meta） | 0.02 s |
| `第二行`（read 卡正文的第一行 = offset=2 生效） | 0.03 s |
| `offset=4`（续读提示 = 2 + 2 行） | 0.03 s |
| `读到了。`（收尾答复） | 0.03 s |

  落盘文件内容与 `write` 参数逐字相同；库里那条 `tool_result` 的 `details` 与 `duration_ms` 都在。
  中途踩到一个自己挖的坑：release 二进制没重编 → 库里记的是 `unknown tool: write`（**改动 `src` 后必须 `cargo build --release`**）。

### 15.7 这一轮之后还没做的（按价值排序）

1. **工具进度事件**（长命令流式输出：`run_shell` 现在等子进程退出才一次性拿输出，要改成边读边转）＋ 渲染侧 `is_partial`/`spinner`；
2. **渲染注册表**：把 `render/tools.rs` 里剩下的 match 分派收敛成 `name → renderer` 表 + 兜底，一工具一文件（omp 的形状）；
3. **`Action` / `Event` 分层**：`SessionEvent` 现在 12 个变体里用户侧 3 个、服务侧 9 个，切开；
4. `ToolRequest.text`（工具调用前那半句）上屏；
5. 斜杠命令表从 TUI 挪回服务侧（附录 D）。

---

## 16. 第八轮：工具进度（长命令不再像卡死）

### 16.1 一句话

`bash` 原来等子进程退出才一次性拿输出（`wait_with_output`），所以一条跑 30 秒的命令在屏幕上
等于冻结。现在边读边转：输出按**行**分块、节流 120 ms 推到前端，实时画在待定卡片下方。

### 16.2 通路（五段，每段都只做一件事）

| 段 | 位置 | 做什么 |
|---|---|---|
| 工具 | `run_shell` | stdout/stderr 各一个读线程，**按整行**重组后发进一条 channel；工具线程边等边转发（`ToolProgress::Output`，120 ms 节流） |
| 循环 | `loop_rs` | `ToolExecutor::execute(call, progress)`；`ToolEvent::Progress { call_id, progress }` 原样转发，不解释 |
| 会话 | `session.rs` | 累进 `StreamView.tool_output`，**有界**（留最后 16 KiB，截断处插 `…`），`Change::Stream` |
| wire | `ServerMsg::Stream.tool_output` | 随流式快照走（≤30 Hz 那一路），`#[serde(default)]` 兼容老前端 |
| TUI | `live_tail` → `tools::output_lines` | 纯文本画最后 12 行（命令输出不是 markdown：`*` 必须原样），排在待定工具卡下面 |

三个设计点，都是刻意的：

- **按行而不是按字节块**：一次 `read` 的边界既不是字符边界也不是行边界，直接转发会把一个多字节字
  符或一段转义序列切成两半。读线程把尾巴留在缓冲里，凑齐一行才发。
- **增量而不是快照**：命令打印 10 MB 时，快照方案每帧都要重抄一遍已经打过的内容；增量只付新增的那点。
- **有界而不是全留**：实时槽是**显示缓冲**不是转录——跑着的命令只关心"刚打印了什么"，
  完整输出由结束时的结果条目（以及巨物）负责。

### 16.3 验证

真实进程 + pty（伪网关让模型跑 `for i in 1 2 3; do echo 第$i行; sleep 0.4; done`，
`/tmp/mypi-fake/progress_check.py`）：

| 屏幕上第一次出现 | 时刻 |
|---|---|
| `第1行` | 0.17 s |
| `第2行` | 0.42 s |
| `第3行` | 0.82 s |
| `跑完了。`（收尾答复） | 1.23 s |

三行各自**在命令还在跑的时候**到达（命令 1.2 s 才结束），不是结束才一次性冒出来 —— 这正是
「每秒打印一个 1、持续 30 秒」那种命令要的效果。

单测：`a_long_command_reports_while_it_is_still_running`（第一块进度必须在调用返回**之前**到，
且内容是第一行）、`a_running_tools_output_lands_in_the_live_slot_and_stays_bounded`
（累进、有界、截断有标记、调用结束即清空）、`a_running_tools_output_rides_the_tail_as_plain_text`
（`*不是强调*` 原样上屏、只画最后 12 行、结束后消失）。627 测试 / clippy 0 警告。

顺带说明事件层的现状（用户要的「标配 start/end + 允许自定义」）：

- **start / end 是循环对每次调用的保证**（`ToolEvent::Start` / `Finish`），不写在工具定义里——
  工具也就无从漏掉；
- **自定义事件**在 `ToolSpec.events` 里声明（今天是空表，因为唯一的自定义事件 `progress`
  是所有工具共用的通用通道），落地形态是 `ToolProgress` + `ToolEvent::Progress`。

---

## 17. 第九轮：工具渲染收尾（注册表 + 抄 omp 的画法）

### 17.1 一个工具 = 一个 impl + 一行注册

`tui/.../history/render/tools.rs` 里原来有 **9 处 `match name`**（折叠阈值、折叠丢哪头、meta、
结果量词、调用正文、结果段落、diff 判定……）。现在是一张注册表：

```rust
pub(super) trait ToolRenderer {
    fn fold_limit(&self) -> usize { DEFAULT_FOLD }
    fn fold_takes_head(&self) -> bool { false }
    fn meta(&self, b: &ToolBlock<'_>) -> Vec<String> { Vec::new() }
    fn result_measure(&self, o: ToolOutcome<'_>) -> Option<String> { /* N lines */ }
    fn call_body(&self, b: &ToolBlock<'_>, t: &HistoryTheme) -> Vec<Line<'static>> { /* JSON */ }
    fn result_sections(&self, b, o, t) -> Vec<Section> { /* 普通输出 + 折叠 */ }
    fn is_diff(&self, o: ToolOutcome<'_>) -> bool { /* 读 details */ }
}

fn renderer_for(name: &str) -> &'static dyn ToolRenderer {
    match name { "bash" => &Bash, "edit"|"mass_edit" => &Edit,
                 "read"|"write"|"cd" => &FileTools, "fetch"|"search"|"browser" => &Web,
                 _ => &Fallback }
}
```

`ToolBlock` 只做委派；`meta` 的「结果规模 + 耗时」尾部是所有工具共用的，留在 `ToolBlock` 里。
形状照 omp 的 `packages/tui/src/tools/renderer.ts`（接口 + `toolRenderers` 表 + 兜底渲染器）。

### 17.2 抄了 omp 的三处画法

| 工具 | 抄什么 |
|---|---|
| `write`（新） | omp `write.ts` 的 `renderContentPreview`：**行号栏 + 按目标语言高亮的内容**，收起时折到 8 行并给 `… N more lines`。以前走兜底渲染器（一行 JSON 参数） |
| `read` | header 的 meta 带**续读区间** `path:11-20`（从 `details` 的 offset/lines 算），说明这次看的是哪一段、上次停在哪 |
| `edit`/`mass_edit` | diff 行 `- old` / `+ new` 真的活过来了（见 §15.3：此前那条路只有测试桩在走） |

### 17.3 工具调用前那半句（`ToolRequest.text`）终于上屏

它作为**第一条调用**的一部分存档，现在画在卡片**上面**当正文（只认 `first`：一条消息带多个
调用时那半句不属于后面几次）。此前只在流式尾巴上闪一下，`buffered` 模式下**完全不上屏**——
数据一直在，只是没人画。

### 17.4 验证

真实进程 + pty（伪网关驱动 `write` → `edit` → 收尾，`/tmp/mypi-fake/cards_check.py`），
最终屏幕逐行：

```
我先写个文件
+- [ok] write: 写个笔记 · /tmp/mypi-fake/work/notes.txt -+
|  1 第一行                                              |
|  2 第二行                                              |
...
+-- ---------------------------------------------------- +
| 已新建 /tmp/mypi-fake/work/notes.txt（60 字节）         |
+-- ---------------------------------------------------- +
再把第三行改掉
+- [ok] edit: 改一行 · /tmp/mypi-fake/work/notes.txt --- +
| - 第三行                                               |
| + 第三行（改过）                                        |
+-- ---------------------------------------------------- +
都好了。
```

单测 627 → **633**（新增：write 的行号栏预览与折叠、read 的续读区间、兜底渲染器、
那半句画在卡片上面且只画一次）；clippy `--all-targets` 无警告。

**没做的两项**（诚实记账）：`is_partial` 没有做成渲染器选项——「还没跑完」这件事现在是
「待定卡片 + 实时输出尾巴」那条路表达的（见 §16）；spinner 动画没做（没有逐帧字形）。

---

## 18. 第十轮：斜杠命令接线（服务端表 + wire 分发）

### 18.1 现状的病灶

`tui/.../reserved/completion/engine.rs` 里那张 `COMMANDS` 表**只驱动补全**：全仓库没有分发，
回车提交 `/name foo` 时整串文本被当作**用户消息**发给模型——十个命令一个都不生效，还白烧 token。

### 18.2 分法（照 omp：元数据 UI 无关 + handler 在服务端 + 两个前端共用一张表）

`src/server/commands.rs`（新）：

```rust
pub struct CommandSpec {
    pub name: &'static str,
    pub aliases: &'static [&'static str],
    pub detail: &'static str,   // 补全与帮助的唯一来源
    pub args: ArgKind,          // None | ModelId | Path | Text（补全与校验共用）
    pub scope: Scope,           // Session（服务端跑）| Local（前端自己的事）
}
pub fn lookup(name) -> Option<&'static CommandSpec>;
pub fn split(input) -> Option<(&'static CommandSpec, &str)>;  // `/name foo` → (spec, "foo")
```

表里 8 条（含别名）：`/name` `/cdp`(=/cd) `/compact` `/switch` `/profile` 归**服务端**；
`/model` `/resume` `/q`(=/quit/=/exit) 归**前端**。一个名字只能有一个归属——把 `/cdp` 放前端做
就和「服务端持有 cwd 真相」打架。

### 18.3 通路

| 段 | 做什么 |
|---|---|
| `ServerMsg::HelloOk.commands` | 握手时把**表**下发（`CommandInfo`：名字/别名/说明/参数形状/归属）。前端拿它做补全与帮助，不再自己存一份会漂的副本 |
| `ClientMsg::Command { name, args }` | 前端只做**识别**，意义在服务端 |
| `Daemon::command` | 查表 → 会话命令调 `Session::run_command`；失败**叙述**成一条 `Entry::System`（不炸线、不静默） |
| `Session::run_command` | `/name` → `NameMarker`；`/cdp` → `SetCwd`（顺带 `bump_cwd_seq`）；`/compact` → `run_compact`；其余如实说「还没接线」 |
| TUI 提交路径 | `split()` 认出来：Session 走 wire、Local 当场办（`/q` 退出）、**未知 `/xxx` 明确提示**（以前静默发给模型） |
| 草稿态 | 命令和提交一样**按需建会话**（`ensure_session`）——打 `/name` 的意图不比打一句话含糊 |

### 18.4 验证（真实进程 + pty）

输入 `/name 测试会话`（伪网关在跑，但**不该**收到请求）：

```
屏幕上出现：会话已命名为「测试会话」    0.01 s
库里：     seq=1  kind=name  {"name":"测试会话"}
```

命令被识别、走了 wire、会话被按需创建、改名落盘、结果作为一条 System 通知回灌转录——
而且**没有**变成发给模型的用户消息。

### 18.5 补全表也换成服务端那份了（副本消除）

TUI 里那份 `COMMANDS` 常量**删掉**：`complete_commands` / `find` / `leaf_state` 全部改成
**接收表参数**，表由握手下发。链路：`hello_ok.commands` → `ClientConn`（握手在 `connect`
里被消费掉，所以表存在连接上，`conn.commands()` 取）→ 主循环 `reserved.set_commands(…)`
→ 补全服务 → 控制器。

顺带修掉一个真 bug：`is_exact_command` 原来只比名字，**别名不算命中**——`/quit` 打全了
不会一步提交。现在名字与别名都算。

验证（真实进程 + pty）：输入 `/na` 再按 Tab → 输入行变成 `/name`（0.01 s）。这个 `/name`
来自服务端的表，前端不再有第二份。

### 18.6 这一轮没做完的

1. `/switch`（需要模型解析器）、`/profile`（需要 profile 根）、`/model`（需要写 config.yaml）
   目前是**明确的「还没接线」提示**——不装作办了。
3. `/resume`（本地，要画会话选择器 overlay）没接。

---

## 19. 附录 G：斜杠命令规范（现状梳理 + Action 模型 + 注册成本）

### G.1 现状：通路五段

| 段 | 位置 | 做什么 |
|---|---|---|
| 表 | `server/commands.rs` | 8 条：`name` / `aliases` / `detail` / `args`(ArgKind) / `scope`(Session\|Local) |
| 下发 | `ServerMsg::HelloOk.commands` → `ClientConn`（握手在 `connect` 里消费掉，表存在连接上） | 前端拿它做补全与帮助，**不再自己存一份** |
| 上行 | `ClientMsg::Command { name, args }` | 前端只做**识别**，意义在服务端 |
| 执行 | `Daemon::command` → `ensure_session`（草稿态按需建会话）→ `Session::run_command` | 会话命令在这里落地 |
| 回执 | 成功/失败都回一条 `Entry::System` 通知（内存、不落盘） | 失败**叙述**原因，不炸线不静默 |
| 本地 | `loop.rs::local_command` | `/q` 这类前端自己的事 |

### G.2 Action 要不要做成广播？—— 不，而且"广播"那一层已经有了

omp 的证据（`packages/coding-agent/src/slash-commands/types.ts`）：命令 handler 拿到的是
**能力门面**，不是总线：

```ts
interface SlashCommandRuntime {           // text/ACP 模式的 handler 用
  session: AgentSession; sessionManager: SessionManager; settings: Settings; cwd: string;
  output: (text: string) => ...;          // 对操作者说话（TUI→showStatus，ACP→sessionUpdate）
  refreshCommands(): ...; reloadPlugins(): ...;
}
interface TuiSlashCommandRuntime {        // TUI-only handler 用
  ctx: InteractiveModeContext; input?: ...; draftDetached?: boolean;
}
type SlashCommandResult = undefined | { consumed: true } | { prompt: string };
```

**请求不能广播**：变更必须有唯一归属——`/name` 广播给谁听谁改名，就是 N 个前端各改一次。
**能广播的是效果**，而这一层我们已经有了两层：

| 层 | 形状 | 订阅者 |
|---|---|---|
| wire | `ServerMsg::{Entry, Stream, State}` | 任何前端（TUI / 未来的 web / telegram） |
| TUI 内部 | `StatusEvent`（`input/statusline/mod.rs`） | **7 个状态栏组件各自订阅**：`ModelChanged` / `WorkspaceChanged` / `CwdMigrated` / `SessionRenamed` / `Git` / `Usage` / `Tick`+`Activity*` |

也就是说你设想的「谁听到对应的 Action 去做对应的事」**已经在状态栏这一侧跑着了**：`model.rs`
只听 `ModelChanged`，`git.rs` 只听 `Git`，`cwd.rs` 只听 `WorkspaceChanged`/`CwdMigrated`。
状态栏的输入分两类，与分工铁律一致：

- **服务端事实**（`view.rs::on_state` 从 `State` 帧派生）：模型显示名、会话名、cwd、花费、上下文占用；
- **本地事实**（`loop.rs`）：git 快照、启动时从 config 解析的显示名、启动目录。

要改的只有一处小重复：`ModelChanged`/`WorkspaceChanged` 在 `loop.rs`（启动 bootstrap）与
`view.rs`（状态帧）各发一次——命令接线后服务端才是唯一写者，启动那次是补初始值，注释写清即可。

### G.3 注册成本：加一条命令现在要动几处

| 场景 | 改动 | 自动生效的部分 |
|---|---|---|
| 会话命令 | `commands.rs` **一行** + `Session::run_command` **一个 arm** | 补全（表下发）、wire、TUI 识别、失败叙述 |
| 本地命令 | `commands.rs` 一行 + `local_command` 一个 arm | 同上 |
| 需要新会话能力 | + `Session` 上一个方法 | |
| **需要配置** | ✗ 摩擦点：`/switch` 要模型解析器、`/profile` 要 profile 根、`/model` 要写 config.yaml | 建议给 `SessionSpec` 加 `CommandEnv`（闭包/句柄集中声明"命令能用什么"） |

现状的三个缺口：

1. **参数形状只用于补全**，服务端不校验（`ArgKind` 应参与校验，现在靠每个 arm 自己的 `ensure`）；
2. **结果只有一种**（`Entry::System` 文案）——`/compact` 想报 tokens 前后只能把数字拼进句子；
   要给命令结果加结构化载荷的话，正好和工具那套 `details` 同形；
3. **没有 `/help`**：表里有 `detail` ✓ 但没有命令把它列出来。

### G.4 验证 `/compact` 挖出的一件事（以及我两次读错屏幕）

**真 bug：叶子命令的回车被白吃（已修）。** 行首斜杠词会挂起（`hang`，把补全解释权让给
保留区），于是回车先被当成"确认候选"投给保留区。`/compact` 这种**无参数命令**是叶子
（精确命中 → 补全弹窗关闭），此时没有候选可确认，那一下回车就**被吃掉**了——命令永远
发不出去，得按两次回车。修法：`MainZone::deliver` 的 `ToConfirm` 分支在保留区没有候选
可确认时把回车**落回提交**（`InputZone::submit_now`）。单测
`a_leaf_command_submits_on_the_first_enter` 守着它（修前必失败）。

**我两次读错了屏幕（都已改正）。**

1. 先报"回合结束后 TUI 不再接受输入"——**错**。那是我的 pty 脚本拿原始字节搜多字节字符，
   而 TUI 的差分渲染是**逐字符带光标跳转**输出的（`\x1b[27;1H没\x1b[27;3H有…`），连续字节
   永远搜不到。换成只打字符、不解析屏幕的探针后：回合后打字正常上屏。
2. 又报"`/compact` 的通知转录里有、屏幕上没有"——**也错**。同一原因。把最后一帧的原始字节
   存下来看，第 27 行明明白白是 `没有可压缩的历史（尾部已覆盖全部条目）`。

**`/compact` 端到端是对的**（证据链）：命令到达 daemon ✓ → `run_compact -> true` ✓ →
后台如实拒绝（`没有可压缩的历史（尾部已覆盖全部条目）`，2 条消息的会话本来就没得压）✓ →
落进转录 ✓（单测 `a_refused_compaction_still_reaches_the_transcript`）→ 上屏 ✓（原始帧第 27 行）。

教训写在这里：**pty 回读屏幕不能拿原始字节做子串匹配**，要么用 Screen 模型逐帧重建，
要么把帧 dump 下来人眼确认。前几次能"看到"是因为那些字符串恰好没被光标跳转切开。

## 20. 第十一轮：`todo` 工具（模型的**外部备忘录**）

### 20.1 为什么状态必须在会话里（我第一版设计错了）

我最初的"无状态版"（模型每次重发全量清单）是错的：那样模型得先在脑子里维护整张表才能重发，
而压缩又可能把"最后一条工具结果"摘要掉——备忘录就没了。**todo 的用途恰恰是不让模型用脑子记**。

照 omp 的形状重做（`packages/coding-agent/src/tools/todo.ts` 的四个决定）：

| 决定 | 我们这边 |
|---|---|
| 状态在会话里，不在模型脑子里 | **`Entry::Todo { phases }`**，一等转录条目（和 `Entry::Compaction` 同待遇：落库、走 wire、可回放）；"当前清单" = **最后一条** |
| 模型只发**操作 + 那一项的原文** | `{op, task}`；`task` 必须与写下时逐字一致 |
| 每次结果都带**全量清单** | 给模型的文本是全量清单，`details.phases` 也是全量 → 它随时能重读自己的备忘录，另有 `{op:"view"}` |
| 一批有错**整批丢弃** | 状态一动不动，理由逐条回报（一次改完就能过） |

### 20.2 形状

- **9 个操作**：`init`（建/换整份）/ `append` / `start` / `done` / `drop` / `block`(带 reason) / `unblock` / `rm` / `view`；
- **状态**：`TodoPhase { name, tasks: [TodoTask { content, status, blocker? }] }`，
  `status ∈ pending | in_progress | done | blocked | abandoned`；
- **一次只能有一项 in_progress**：`start` 自动把上一项退回 `pending`（三件"当前"等于没说）；
- **卡片**：整份清单 + 每状态一个字形（`[ ] [>] [x] [!] [-]`，ASCII 预设）+ header 的 `op · N/M done`；
  `Entry::Todo` 本身**不画**（状态不是叙述——可见的记录是那次调用的卡片，和 `Entry::Name` 同一个道理）。

### 20.2b 用户补的一条规范：`cd` 临时、`/cdp` 落盘且**追加**

（用户口述，记在这里备用，不一定本轮实现。）

- 模型的 **`cd` 是临时的**：重启回到上一次的目录。它**不进元数据**（工具调用的*结果*照旧留在转录里）；
- 用户的 **`/cdp` 才落元数据**，而且是**追加**不是替换：A → B → C 之后，这段对话可以在
  **A、B、C 任何一个目录**启动；
- `/cdp` 要向服务端追加一条消息，服务端再给上下文加一条"当前工作区正式转变"。

现状对照：`Entry::Name`（名字）已经是"追加 + 最后一条生效"的形状 ✓，`/cdp` 现在写的是
`SessionEvent::SetCwd { seq, path }` + `cwd_history` 表（已有 seq 的追加语义 ✓），缺的是
"**启动时让用户挑一个历史工作区**"这一步，以及上下文里那条"工作区正式转变"的注记。

### 20.3 通路（复用 `cd` 那套"共享槽"）

```
工具 run_todo  →  写 BuiltinTools 的 todo 槽（current / pending）
回合结束      →  tools.take_todo() → SessionEvent::Todo { phases }
会话          →  Entry::Todo（落库）+ Change::Transcript
下一次回合    →  从 state.current_todo() 播种回槽
上下文        →  entries_to_context 追加一条「任务清单（当前状态）」注记
```

最后那一步是关键：**从压缩点开始的上下文也带清单** ✓ 否则模型压缩完就"忘了"自己在做什么。

`current_todo()` 还有个兜底：`Entry::Todo` 是**回合结束**才落的，而卡片当场就落库；回合中途
死掉（断网、Esc）时从最后一条 `todo` 工具结果的 `details` 里再捞一次——备忘录不跟着丢。

### 20.4 验证

单测 **640 → 655**：9 条工具逻辑（init 两种形状 / 逐字查找 / start 退位 / block 带原因 / append 拒重 / rm 连空阶段 / view 只读 / 报错文案 / 摘要永远全量）、
3 条端到端（工具读到了**没被告知**的状态；有错不发布；回合发布 + 上下文注记）、1 条兜底（回合死了也能捞回来）、2 条渲染（五态字形 + 进度）。

真实进程 + pty（伪网关连打 `init → start → done → view`），屏幕逐行：

```
先把清单立起来
+- [ok] todo · init · 0/3 done ------+
| 斜杠命令                            |
| [ ] 补全表挪服务端                  |
| [ ] 接 /switch                     |
| [ ] 写测试                          |
+-- ------------------------------- -+
开始第二项
+- [ok] todo · start · 0/3 done -----+
| [>] 接 /switch                     |     ← 状态跟着走
第二项做完了
+- [ok] todo · done · 1/3 done ------+
| [x] 接 /switch                     |     ← 而且 header 报 1/3
看看现在的清单
+- [ok] todo · view · 1/3 done ------+
清单维护好了。
```

库里（`sessions.db3`）：每条工具结果的 `details` 带全量清单 ✓，一条 `Entry::Todo` 落在回合末
（`10 todo [('斜杠命令', [('补全表挪服务端','pending'), ('接 /switch','done'), ('写测试','pending')])]`）✓。

**外部备忘录的证据**：`start` / `done` 两次调用只发了 `{op, task}`，卡片里却是**全量清单**——
状态来自会话，不是来自模型重发。

---

## 21. 第十二轮：`/switch` `/profile` `/model` 接线（+ 一个静默失效的 bug）

### 21.1 命令怎么拿到会话外的东西：`CommandEnv`

会话层本来一个依赖都没有（不认识 provider、不认识 models.yml、不认识 config.yaml）。
三条命令都需要外面的一点东西，于是 `SessionSpec` 多带一个 `CommandEnv`：

| 槽 | 形状 | 谁填 |
|---|---|---|
| `resolve_model` | `Fn(id) -> Result<ModelSwitch, String>` | `main.rs` 从 `Config` 构造 |
| `resolve_profile` | `Fn(name) -> Result<(system, roster), String>` | 同上（**先对花名册**，见 §21.3） |
| `set_default_model` | `Fn(id) -> Result<去哪了, String>` | `Config::set_default_model` |

`ModelSwitch` 是**压平**的结果：endpoint、key、id、显示名、上下文窗口、输出上限、价目表。
会话层拿到就换，不再需要问任何人——`Session::switch_model(&Config, …)` 那种签名（会话层
自己拿 `Config` 去查 provider）已经被删掉了，它当时没有任何调用者。

`None` 的语义是"这一侧没接线"：命令会**明说**"模型解析器没接"，而不是静默无操作。

### 21.2 `/model` 的两个落脚点

1. **本会话的元数据**（立刻生效）：client 的 endpoint/key/id + `cost`/`context_window`/
   `model_name`/`max_tokens` 一起动。半套旧元数据比不换更糟——量表按旧窗口算，屏幕上写着新名字。
2. **配置文件里对应的键**（下次启动生效）：`Config::set_default_model`。

写入是**认键不认行号**的：逐行找**顶层**的 `default:`（缩进的属于某个小节，动它就
是在改别人的配置），只换那一行，其余字节原样。不 YAML 往返——那会把用户的注释和键序吃掉。
写成接口（`CommandEnv::set_default_model`）而不是"会话层知道 config.yaml 在哪"，因为
配置层日后要重构；到时只改 `Config::set_default_model` 的函数体。

**顺序是契约**：先解析 id，再落盘。id 不存在就一个字节都不写——这条命令最容易造成的
伤害就是把用户的下一份配置写坏。

### 21.3 `/profile`：`resolve` 是宽松的，用户打的名字必须对花名册

`profile::resolve` 的文档写着它是启动那条路（配置里写了个还没建的 profile 就当内置用），
"missing profile names never fail silently **here** — the caller surfaces it"。
所以 `CommandEnv` 里的闭包先查 `profile::list`：打错名字要**挨骂**，不能静静地"切到"
一个不存在的 profile。第一次真机跑就抓到了这个：`/profile 没有这个` 回了"切到「没有这个」"。

### 21.4 顺手抓到一个静默失效

装配时发现 `SessionSpec.compact` **没人接**：`Session::with_compact` 写了、有文档注释、
零调用者。也就是 `/compact` 一直用默认旋钮跑——用户改了 `config.yaml → compact:` 却
毫无变化，**没有任何报错**。已在 `SessionHub::assemble` 里接上，并加了一条防复发的
装配测试（`the_spec_reaches_the_session_it_specced`）：这类失效只有测试能守。

### 21.5 验证

单测 **655 → 660**（`/switch` 三条 + `/profile` + `/model` 的顺序契约 + 装配契约 + 写入器的
注释/键序/新建/缺键四种情形）。

真机（`slash_check.py`，pty 里敲命令读屏幕），九项全过：

```
/switch fake:model-b  → 本会话模型切到 FAKE-B（model-b）——对这次会话有效，落盘用 /model
                        状态栏：[M] FAKE-A → 之后 [M] FAKE-B，分母 0%:128K → 0%:64K
/model  fake:model-b  → 模型切到 FAKE-B（model-b），并已写为默认：/tmp/mypi-fake/config.yaml
/profile default      → profile 切到「default」（系统提示词已换；工具名册：全部）
/profile 没有这个      → /profile 没做成：未知 profile：没有这个。可用：default
/switch  fake:不存在   → /switch 没做成：provider fake has no model `不存在`
换完之后说句话          → 好，我记下了。（请求确实打到了新模型）
```

`config.yaml` 前后（注释与无关键原样）：

```yaml
 # 这是用户的注释，别动它          # 这是用户的注释，别动它
 theme: {}                        theme: {}
-default: fake:model-a            +default: fake:model-b
 tools:                           tools:
   bashTimeoutSecs: 33              bashTimeoutSecs: 33
```

---

## 22. 第十三轮：`/resume` 全屏会话选择器（第一个非主区 Zone）

样式照 omp 的 `packages/tui/src/overlays/session-selector.ts`（1137 行）：
标题栏 + 搜索框 + **一条会话一块**（标题行 / 首条消息预览 / 元信息行）+
底部按键提示。框线不新造——直接用卡片原语（`render::cards` 提为
`pub(crate)`），所以选择器和工具卡片是一套视觉，第二套边框约定不存在。

### 22.1 三处联动（用户点名的三件事）

| 联动 | 在哪 |
|---|---|
| `/resume` 命令 → 所有权从 Main 交到 Resume | `local_command` → `App::open_resume(cwd)`（过账本登记） |
| Zone 自己解释键位 | `ResumeZone::deliver`（↑↓ / PgUp/PgDn / Home/End / Enter / Esc / Tab / 退格 / 可打印字符筛选 / Ctrl+U） |
| 与服务端交接 | 页面不碰 socket：把意图记成 `Request::{List, Attach}`，主循环取走发 `ListSessions` / `Attach`；`ServerMsg::Sessions` 按账本路由回它 |

**附着后所有权是 Zone 自己交回去的**（`deliver` 返回 `Handover{Main}`）：
Enter 一按，页面退场 + 请求排队，主循环只管把消息发出去，不需要知道
"现在该切回谁"。

### 22.2 服务端：一条查询喂满一页

`store::list_session_rows(under)`：元数据 + 首条用户消息 + 存储字节数，
**一条 SQL**（两个相关子查询）——每条会话一次往返会让一百条列表变成
一百次查询。`under = Some(dir)` 只列在该目录跑过的会话（项目 A 不该看见
项目 B 的），Tab 切全部。这条查询让早就写好、一直没人调的
`list_sessions_under` / `display_name` 变成了活代码。

wire 上 `ClientMsg::ListSessions` 多了 `under`（`#[serde(default)]`），
`SessionInfo` 多了 `first_message` / `bytes`（同样默认值兼容）。

### 22.3 两个判断

- **标题 = 名字，没名字就是首条消息**（omp 的规则）。用户挑会话问的是
  "这是哪次对话"，答案就是首条消息；合成名（`MM-DD:HH-MMSS+前 7 个字`）
  只在"名字和消息都没有"的空会话上兜底。
- **预览行只在有显式名字时画**：没名字时首条消息已经是标题，再画一遍是重复。
- 作用域取 **TUI 进程的 cwd**（和 git 快照同一个来源）。它是"你站在哪个
  项目里"，与会话的 cwd 在 `/cdp` 之后可能不同——这是有意为之，不是疏漏。

### 22.4 验证

单测 **662 → 673**：13 条在 resume 模块（进页面就拉当前目录 / 筛选后回车
附着的是**光标下那条**而不是原列表第 0 条 / Esc 不附着 / Tab 保筛选切作用域 /
导航两端不倒挂 / 筛选空结果再退格能回来 / 空筛选退格不 panic / 标题与预览的
三种情形 + 整页宽度与页脚不被裁 + 三组格式化）。

真机（`resume_check.py`：三次 TUI 跑同一个 daemon，pty 回读屏幕），九项全过：

```
+- 会话 · 2 个 · 当前目录 --------------------------------------------- +
| > 输入关键字筛选（名字 / 首条消息 / 目录）                              |
+-- ------------------------------------------------------------------ -+
| > 第二个会话的暗号                                                     |   ← 选中：accent + 粗体
|   刚刚 · 149 B · …/mypi-fake/work                                     |
|                                                                      |
|   第一个会话的暗号                                                     |
|   刚刚 · 149 B · …/mypi-fake/work                                     |
|                                                                      |
|                                        （下面填满到页脚）               |
+-- ------------------------------------------------------------------ -+
| Enter 附着 · Esc 返回 · ↑↓ 选择 · Tab 范围 · Ctrl+U 清筛选              |
+-- ------------------------------------------------------------------ -+
```

输入 `第二个` → 标题栏变 `1 个`、只剩一条；回车 → 该会话的转录上屏
（`▌ 第二个会话的暗号` + `好，我记下了。`），状态栏回到 `[M] FAKE-B`；
再进一次 + Esc → 回主区，输入框还在，什么也没动。

**注意**：屏幕上页脚显示成 `↑↓ 择`（少一个"选"）是**回读装置的伪影**，
不是软件——单测断言 `render()` 的第 30 行既含"选择"又 ≤110 格。这与
§G.4 记的是同一类坑：pty 差分渲染逐字符定位，回读要按屏幕模型重建，
不能拿原始字节做子串匹配。

---

## 23. 调研：omp 的 handoff 提示词对缓存友好吗？

**结论：友好，而且是刻意设计的**——文档注释里就写着 "cache-friendly"。
它把 handoff 请求做成**主回合的追加**而不是另起一份请求，命中的是主回合
刚填过的 provider 前缀缓存；真正让缓存失效的是它之后那次压缩提交，那是
压缩的固有代价，不是 handoff 的。

### 23.1 证据（全部来自源码，不含推测）

`packages/coding-agent/src/session/session-handoff.ts`：

- 类注释：`/** Generates handoff documents with a cache-friendly oneshot LLM call. */`
- `generateDocument` 的实现注释把机制讲全了：

  > Build the handoff request through the SAME pipeline a live turn uses
  > (`runEphemeralTurn` / `/btw` share it) so the oneshot reads the provider
  > prompt cache the main turn populated instead of cold-missing the whole
  > prefix: identical system prompt, normalized tools, and
  > transform-/obfuscation-matched message history via `convertMessagesToLlm`
  > + `buildSideRequestContext`, plus the live turn's effective provider cache
  > key with a unique side `sessionId` so OpenAI/Codex append-only state never
  > mixes with the live turn.

- 请求体是**追加**：`const handoffSnapshot = [...agent.state.messages, { role: "user", content: handoffPromptText }]`
  ——前缀一字节不动，只在末尾加一个用户回合，这就是前缀缓存能命中的充分条件。
- 缓存路由照抄主回合：`promptCacheKey: this.#host.agent.promptCacheKey ?? sessionId`，
  而 `sessionId` 加后缀隔离：`sessionId: \`${cacheSessionId}:side:${Snowflake.next()}\``
  ——既命中缓存，又不把 OpenAI/Codex 那种 append-only 的会话状态和主回合搅在一起。
- `packages/agent/src/compaction/compaction.ts:1063-1072` 的接口文档同样点明：
  "Sending the same routing + payload shape the main loop uses is what lets the
  handoff oneshot READ the provider prompt cache the live turn populated
  instead of cold-missing the whole prefix."
- 请求只关掉工具：`toolChoice: "none"`，thinking 用压缩档 `resolveCompactionEffort`
  ——都不影响前缀。

### 23.2 什么**不**友好（诚实的那一半）

1. **handoff 之后紧跟的那次压缩提交**必然让下一回合冷启动：它把前缀换成了
   一份文档。这是压缩的定义，不是 handoff 的锅——omp 也明说
   "this method rewrites no history"，它只产出文档，提交归
   `SessionMaintenance`。所以「handoff 省下的」是**这一次总结调用**的输入
   费用，不是之后整段会话的费用。
2. **如果**侧路请求用了别的系统提示词或另一套工具表，就会整段冷失效——
   omp 正是为此才强制走同一条管线。照着做时这一点最容易破功（"顺手换个
   精简提示词" = 缓存归零）。
3. 输出 token 是**额外**的：它不是在已有回复里截一段，而是让模型再生成一份
   结构化文档。压缩的收益（之后每回合少带几万 token）要若干回合才回本。

### 23.3 我们的位置

`src/server/compaction.rs` 的模块说明写的就是同一套：*"Prefix-replay summary
call. The summarization request reuses the live context prefix (system + tools +
compacted region verbatim) and appends the instruction as a final user turn."*
——同一条路，所以我们在这一点上本来就是缓存友好的。**差异**：omp 另发一个
专用的 summarization 系统提示词，我们不发（那会让前缀整段失效）；所以本轮
把 omp `summarization-system.md` 的两条护栏（把历史当不可信数据、不许接着
往下写）**揉进了指令本身**（`server::prompts::COMPACT_INSTRUCTION` 的前两段）。

---

## 24. 第十四轮：一批细节（渲染 / 提示词 / 工具栏 / 启动）

### 24.1 `/resume` 页

- **标题一律加粗白字**：有名字用名字，没名字用首条消息，两样都没有才退回
  合成名。选中行唱 accent。以前用 `Token::SystemText`，那是灰的——标题和
  下面的元信息糊成一片。
- **Delete 两段式**：按 Delete 只在标题后亮出红色加粗的 ` - delete?`，
  **Enter 才真删**；任何别的操作（翻页、换条、打字）撤销确认态。
  真删之后选择器自己重拉列表（列表是外面的事实，不猜）。
- 服务端：新增 `ClientMsg::DeleteSession`，`SessionHub::delete_session`
  **只在回合跑着的时候拒**——开着但闲着是常态（你刚从这个会话出来），
  拒它等于这个功能没用。删之前把观看者摘掉，免得哪个前端的下一次提交去
  resume 一个已经不存在的会话。被拒时错误走 `ServerMsg::Error`，而选择器
  持有屏幕时错误归它显示（`ResumeZone::on_error`）——不然按完 Delete + Enter
  屏幕上什么都没有。

### 24.2 渲染细节

| 项 | 做法 |
|---|---|
| 历史区与状态栏之间 | `MainZone::HISTORY_GAP` = 1：仲裁**扣**掉、渲染**补**上，两处同一个常量；终端矮到只剩一行历史时不硬留（`gap = 0`），否则会把仅有的内容挤掉 |
| 状态栏进度条 | `>-----8%---------:1M`：百分数**嵌在**横线里，左边那段（用量）accent、右边不是。以前百分数排在横线**外面**，读起来像"用了 0%" |
| 草稿会话名 | `--resume`/启动态还没有 session id 时显示「新会话」，只在内存里（服务端要到第一次 submit 才建行），真实名字随第一个 State 帧覆盖 |
| todo 面板 | 抄 omp 的树：罗马数字编号、`|--`/`'--`/`` `---- ``、行号栏式的缩进、收尾的阶段折成 `II. 名字 · 3/3`、活着的展开、完成加删除线。**贴在历史区底部**（占自己的行高，聊天窗口相应变矮，上限半个视口），所以聊得再长也一眼看得见 |

真机（`command_check.py`，pty 回读）：

```
19|清单维护好了。
20|TODO
21| '-- I. 斜杠命令 · 1/3
22|   |-- [ ] 补全表挪服务端
23|   |-- [x] 接 /switch
24|   '-- [ ] 写测试
25| `----
26|                                    ← 历史区与底部之间那一行空行
27|+-- π > [M] FAKE-B > [D] mypi-fake/work >0%----------------:64K | < work --+
```

行号栏（`edit` / `read`）：`-12│旧的一行` / `  +│新的一行`（标记列与行号列
**一起**右对齐，与上一行行号相同才留空——omp 的去重规矩）；`read` 把正文的
`   12<TAB>内容` 重画成 ` 12│内容`（给模型的文本不变，它那边行号 + 制表符
是对齐依据）。`details` 因此多了 `atLine`（老 payload 没有 → 只画标记列，
绝不编一个行号）。write 的行号栏最小宽度对齐 omp 的 `WRITE_GUTTER_MIN_WIDTH`。

### 24.3 提示词（新模块 `src/server/prompts.rs`）

- **`oh-my-pi` profile**：omp 的系统提示词，渲染后（无 Handlebars 标记）、
  去掉我们这儿不存在的东西（技能/规则注入、工具清单、子代理委派、
  浏览器/桌面控制）。行为条款（工作流、交付契约、红线）逐字保留。
  它是**内置**的：没有 profiles 目录也挑得到；用户在 profiles 里建同名目录
  就是显式覆盖。
- **compact 指令**换成 omp 的 `compaction-summary.md`，并把
  `summarization-system.md` 的两条护栏（把历史当不可信数据、不许接着往下写）
  揉进指令本身——理由见 §23.3：我们不发专用的 summarization 系统提示词，
  那会让前缀缓存整段失效。
- 压缩后的包装也换成 omp 的 `compaction-summary-context.md` 形状。
- handoff 的缓存调研见 **§23**。

### 24.4 `mypi --resume`

命令行早就认这个 flag（`Cli.resume`），但没人用——现在：启动 Zone 直接是
`Resume`，并且**进主循环之前**先把列表请求发掉（主循环要等信号才走到发送
那一步，不先发的话第一帧是空白，用户得先敲个键才看得见列表）。

真机（`flag_resume_check.py`，一个字都不敲）：

```
[ok] --resume 直接进选择器
[ok] 没敲键就有列表（不是空白）
[ok] 有按键提示
[ok] Esc 回主区          → 状态栏此时是 `… | < 新会话`（草稿名）
```

### 24.5 验证口径

单测 **673 → 686**（选择器两段式删除与撤销、删除策略、todo 树形与高度
自洽、行号栏、罗马数字、进度条新形状、装配契约）。
真机三套：`command_check.py`（todo 面板 + 空行 + 进度条）、
`resume_check.py`（12 项：列表 / 筛选 / 附着 / Esc / Delete 确认 / 删完真的
从库里没了）、`flag_resume_check.py`（`--resume`）。

---

## 25. 第十五轮：三个真 bug（顺序 / 光标 / 刻度）

### 25.1 时间线倒置：思考跑到工具后面

**现象**（用户实测）：一轮里"边想边调工具"被画成

```
工具调用 / 工具调用 / 思考 / 正文
```

**根因**：`Entry::Reasoning` 只有**一个**产出口——`SessionState::finalize_round`，
而它在**整个回合结束时**才跑。工具条目则在回合进行中就 `push` 进转录了，
于是推理必然排在所有工具后面。

**修法**：`ToolStart` 里，在推入工具条目**之前**先把推理缓冲折成条目
（只在本条消息的**第一个**调用上做，因为推理属于那一处）。回合末尾的正文
照旧在 `finalize_round` 落——于是顺序自动变成
**思考 → 工具 → 思考 → 正文**。

真机（`gateway_order.py`：推理 → 工具调用 → 推理 → 正文）：

```
 9|▌ 写个文件
12|想想先写什么          ← 思考 1（在工具之前）
14|我写个文件            ← 该消息的正文
16|+- [ok] write: … -----+
23|写完了再看看          ← 思考 2（在工具之后）
25|写好了。               ← 正文
```

### 25.2 光标高一格

**根因**：为"历史区与状态栏之间那一行空行"加 `HISTORY_GAP` 时，我扣了行高、
补了空行，**漏了第三处**：`MainZone::cursor_position` 仍把 `heights.history`
当输入容器的起点递下去，而容器实际从 `history + gap` 开始。

**修法**：`container_y = heights.history + heights.gap`。

**更要紧的是那条测试**。原来那条只断言"光标在容器范围内"——容器两行时，
错一行的光标照样"在范围内"，所以它**放过了这个 bug**。现在改成：从
**画出来的缓冲区**里找到输入内容那一行，断言光标行**等于**它。
先回退修法验证过：报 `光标 17，内容画在 18（差一格就是行高算错了）` ✓。

### 25.3 进度条：0% 也该有第一格刻度

`>` 右边**至少一格** accent 横线（0% 时它是"刻度起点"），百分数落在它之后：

```
>-0%----------------------------------:64K      ← 0%
>---8%--------------------------------:64K      ← 8%
```

以前 0% 时 `>` 右边一格 accent 都没有，"0%" 和"这条量表没接上"看起来一样。

### 25.4 验证

单测 **686 → 688**（时间线顺序一条、0% 刻度一条；并**强化**了光标那条：
回退修法时必失败）。真机 `order_check.py`（新网关 `gateway_order.py`）＋
`command_check.py` 复核。

---

## 26. 第十六轮：把三条命令的**参数补全**接上（顺带一个真 bug）

命令表只说"这个参数是什么形状"（`model_id` / `profile_name`），**合法值**
得有人送进来。之前的问题有两层，第二层是这轮才露出来的。

### 26.1 第一层：候选表的线根本没接

- `CompletionService.models` 有字段、有消费方（`model_items` ✓），但**没有
  生产者**：`models_from_config()` 零调用者，`models` 恒为 `None` ✗——所以
  `/switch ` `/model ` 按 Tab 什么都不会弹。
- `/profile` 的参数形状是 `ArgKind::Text` ✗——翻译表直接把它归到"自由文本，
  关弹窗"，profile 名连候选的入口都没有。

**修法**：

1. `ArgKind::ProfileName`（服务端命令表）→ `"profile_name"`（wire 字符串，
   老前端认不出会退化成"没有候选" ✓）→ `engine::ArgKind::ProfileName` ✓；
   补全服务加一条分支：花名册里按前缀过滤，insert 是完整一行 `/profile <名>`。
2. `CompletionService::set_candidates(models, profiles)` ✓，`ReservedZone`
   直通 ✓，`loop.rs` 在补全服务上岗后从**已加载的配置**里造两张表
   （`cfg.models()` + `profile::list(cfg)` ✓ 含内置的 `oh-my-pi`）。
   顺手删掉那个零调用者的 `models_from_config()`（它要求一个我们并不持有的
   `Rc<RefCell<Config>>`）。

### 26.2 第二层：弹窗开着，键位却还在编辑器手里（真 bug）

接上线之后真机跑，发现**回车把半截 `/switch ` 发出去了**——命令当场报
"用法"，用户莫名其妙。根因：

- `KeyContext.popup_open` 这个位子**从来没人填**（恒 `false`）→ 语义表里
  "弹窗开着时 ↑↓/Tab/Enter/Esc 归补全"那一整段是**死代码**；
- 输入区只有一个 `hang` 会借键，而 `hang` 的条件是"光标在触动词**里面**"：
  命令名打完、正在填参数的 `/switch ` 已经落到 `hang = false` ✗。

于是弹窗虽然开着，回车落回提交、↑↓ 也动不了高亮。

**修法**：`InputZone` 多一个 `popup_open`，由 Zone 每轮从补全服务同步
（`reserved.completion_open()` ✓），`deliver` 里弹窗开着就把
Tab/Enter → `Confirm`、↑↓/Esc → 原样借出去；`KeyContext::for_input` 也吃
这个位子。

**同一条路上还顺手修了两处**（都是真机/单测里露出来的）：

- 候选过滤只认 `m.id` ✗：Tab 补出公共前缀 `fake:model-` 之后，`model-a`
  不再命中 → 弹窗当场关掉，像"补全把自己弄没了"。现在**光打 id 和完整地址
  都算命中** ✓。
- `Popup::open` 每次都把高亮打回第一条 ✗：而刷新是**每个按键之后**都跑的，
  ↓ 之后紧接着刷一遍就被拽回顶部。现在**同一批候选**保高亮，候选变了才归零。
- `ReservedZone::lend` 投递前重新认一遍持有者：持有者是上一帧 `settle` 认的，
  "弹窗刚开 + 同一个事件批次里又来一个 ↑↓"会撞上旧账本（无人持有）而丢掉。

### 26.3 验证

单测 **688 → 692**：参数形状映射（含"没见过的形状退化成无候选"）、
`/profile` 候选过滤与 insert、候选**经 Zone 真的到得了弹窗**、
**弹窗开着时回车确认候选而不是提交**（↓ 选第二条 → 写回的是第二条）。

真机（`complete_check.py`，pty 回读），五项全过：

```
=== switch ===                       === profile ===            === model ===
+-/switch fake:model-                +-/profile                 +-/model fake:model-
|    > model-a FAKE-A                |    > default              |    > model-a  FAKE-A
|     model-b  FAKE-B                |     oh-my-pi              |     model-b  FAKE-B

=== switch-confirmed ===   ← ↓ 选中第二条后回车
+-/switch fake:model-b
```

---

## 14. 附：本次新增/未改动说明

**新增（测试装置）**：

- `examples/tui_scale_bench.rs` — 进程内全帧压测 + 合成数据生成 + 服务端 load 探针
- `examples/tui_real_bench.py` — 真实进程冷启动 / 常驻内存测量

**修改**：

| 文件 | 改动 |
|---|---|
| `src/server/store.rs` | 新增 `PATH_CTE` 常量（含陷阱注释）；`load_path` 外层 join 改为 `seq IN (SELECT seq FROM path)`；`effective_name` 由「读全段转录再倒查」改为单行 SQL |
| `src/server/wire.rs` | `READ_POLL` / `HELLO_DEADLINE` / `SNAPSHOT_DEADLINE` 三个常量；`is_read_timeout` 判定；新增 `wait_for_within`，`wait_for` / `quit` 改用它 |
| `src/server/wire/tests.rs` | 新增 3 个测试：慢回应不算断开、静默 daemon 报出所等的时长、等待期间丢弃不匹配消息且不丢缓冲区 |
| `src/tui/session/view.rs` | `dispatch` 由 `&ServerMsg` 改为按值接收，各分支去掉 `.clone()` |
| `src/tui/session/loop.rs` | attach 等待改用 `SNAPSHOT_DEADLINE`；`dispatch` 传所有权；读线程复用 `wire::is_read_timeout` |
| `src/tui/zone/main/history/cache/mod.rs` | `BLOCK_BUDGET` 256 → 512；模块文档与常量注释按实测改写 |
| `src/server/wire.rs` | 新增 `LineScanner`（增量找换行，替代逐次从头扫的 `line_len`）；`read_msg` 改用它 |
| `src/server/daemon/mod.rs` | `read_loop` 改用 `LineScanner`；新增 `SNAPSHOT_CHUNK` 与 `chunk_transcript`，`transcript_msg_for` → `transcript_msgs_for`（快照分块发出） |
| `src/tui/session/loop.rs`（第二次） | 读线程的错误文案按错误类型分派（断开 vs 解析失败） |
| `src/tui/zone/main/history/cache/mod.rs`（第三次） | `BLOCK_BUDGET` 512 → 256；模块文档与常量注释改成实测的「满 256 块 ≈ 18 MB」；新增 `height_or_measure`（走查只取高度，不再重画经过的块）与 `slot_of` |
| `src/tui/zone/main/history/mod.rs` | 新增 `#[doc(hidden)] cache_stats()`，让压测能**读出**缓存占用而不是假设它满了 |
| `src/tui/zone/main/history/render/chat.rs`（第四次） | 新增 `live_tail()`：流式尾巴走 `render_block(…, Streaming::Live, …)`，与成品同一条折行路径 |
| `src/tui/zone/main/history/render/blocks.rs`（第四次） | `wrap_rows` 提到 `pub(super)`，让尾巴的折行与缓存逐字节同路 |
| `src/tui/zone/main/history/mod.rs`（第四次） | 新增 `LiveTail` / `LiveRows`（尾巴 + 行备忘）、`Anchor`（未跟随时的视野锚）、`set_live`；`replace_transcript` 清尾巴；`render_rows` 拆出 `cut_window`（切窗前预留尾巴行数）；新增 `block_budget()` |
| `src/tui/session/view.rs`（第四次） | `on_stream` 从空实现改为 `history.set_live(...)`（按值移动，零拷贝）；新增接线单测 |
| `examples/tui_scale_bench.rs`（第四次） | 缓存行打印真实预算而非硬编码 256；新增 `stream` 子命令与 `reply_text`，量「尾巴在视野内 / 视野外」的每帧代价 |
| `src/server/hub.rs`（第六次） | `SessionSpec` 新增 `stream_mode`；`assemble` 建完会话 `set_stream_mode`（`open_new` / `resume` 同一条路）|
| `src/main.rs`（第六次） | `session_spec` 填 `stream_mode: cfg.app.streaming` |
| `src/server/turn.rs`（第六次） | buffered 的交付点从「回合末」改成「每块文本写完」：工具边界上把攒的整份交出去（并修掉由此暴露的『工具调用前那半句被粘进最终回复』）。见 §7.8 |
| `src/server/ai/config.rs`（第六次） | `StreamMode` 文档按「一块一块、不是一回合一回合」重写（并说明它不管条目）；新增 config.yaml 解析测试 |
| `src/server/daemon/mod.rs`（第五次） | `pump` 在事件那一轮之后**对账**一遍转录尾巴（派生状态，不靠 `Change` 提醒）：用户自己那条消息由此在提交那一刻就发出去。见 §7.6 |
| `src/server/daemon/tests.rs`（第五次） | 新增 `the_submitters_own_message_ships_before_the_round_does_anything`：修前失败（用户消息和回复一起到）|
| `src/tui/zone/main/history/mod.rs`（第五次） | 尾巴「没量过」时先量一次再谈跳过（`LiveRows::measured`）；新增属性测试 `offsets_slice_the_canvas_exactly_with_a_live_tail`：修前失败（滚动切片错位）。见 §7.7 |

单测 585 → **605**，全部通过（连跑三轮无 flake）；clippy `--all-targets` 无警告。

### 2.5 流式接线验证（第四轮新增）

伪网关把第二回合的答复拆成三块、间隔 100 ms；pty 里跑真 TUI，用最小 ANSI
模型（只处理 ratatui 会发的 CUP/SGR/EL/ED/备用屏）把字节流还原成屏幕，再看
每个字**第一次出现在屏幕上**的时刻。

```bash
# 伪网关（OpenAI SSE，两回合：先工具调用，再分三块吐答复）
python3 /tmp/mypi-fake/gateway.py &
python3 /tmp/mypi-fake/stream_check.py            # 可加第二个参数：二进制路径
# 对照：把 src/tui/session/view.rs 的 on_stream 改回空实现，重新 release，
# 用同一个脚本跑，三块答复会落在同一时刻。
```

**临时产物**（可删）：`/tmp/mypi-scale/`（各规模 DB + 隔离 XDG 根）、`/tmp/mypi-small/`、`/tmp/mypi-fake/`（伪网关 + 接线验证脚本 + 修复前的对照二进制 `mypi-before`）。
