# 服务端设计（daemon + 前端协议）

一页纸。定的是形状：进程边界、协议、线程、打断、背压、落地顺序。
代码一行没动；这份东西先过你的眼。

---

## 0. 边界法则（先钉死，其余都是从它推出来的）

| 归 server（daemon） | 归前端 |
|---|---|
| 任何网络连接（模型 HTTP、以后别的） | 一条 unix socket（本地 IPC，不是网络） |
| 数据库（读写、会话列表、resume、树、复现） | 渲染、键盘/鼠标 |
| 线程、超时、重试、工具子进程 | 把收到的消息翻译成子区调用 |
| 配置里**影响请求**的一切（凭证、模型、单价、流式开关） | 配置里**只管显示**的（主题）—— 见第 10 节 |

前端**结构上碰不到**网络和数据库：它那两个东西的句柄都不存在。

---

## 1. 模式与启动

```
mypi                      # 默认：连 daemon；连不上就拉一个；新开一个会话；起 TUI
mypi --server             # 只做 daemon，前台跑
mypi attach <id>          # 附着到指定会话
mypi sessions             # 列会话（脚本可用；同样走 socket，不直接读库）
mypi --resume             # 保留：列出会话让你挑，挑完等价于 attach <id>
mypi --model <p:id>       # 保留：本会话覆盖默认模型
```

默认启动流程（按你定的）：

```
1. connect($XDG_RUNTIME_DIR/mypi.sock)
     失败 → spawn 自己（argv = ["--server"]）→ 等 socket 就绪（超时 5s）
2. hello {proto_version, client_name}
     daemon 回 hello_ok 或 error{proto_mismatch}
3. 起 TUI（socket 读线程 → 唯一信号通道；键盘线程照旧）
```

**会话是懒建的**：起来以后前端处于「草稿态」——没有会话、没有 id、库里一行都不写。
用户**发出第一条消息**（或显式 `attach <id>`）时，daemon 才建会话行、才分配 id。

- `mypi` 打开看一眼就退出 = **零数据库写入**，也不会在会话列表里留一条空会话。
- `list_sessions` 看不到草稿（它只列真会话）。
- `attach <id>` 就是附着到一个已经存在的会话（resume 的语义就是这个，没有别的花样）。
- 两个 `mypi` 同时启动不做任何设计：bind 失败就是失败。

---

## 2. 传输与握手

- **unix socket，SOCK_STREAM**，路径 `$XDG_RUNTIME_DIR/mypi.sock`（每用户一个 daemon）。
- **行分隔 JSON**（一条消息一行）。选它是因为：标准库够用、零新依赖、`socat` 就能手撸调试、解析写法跟我们现在的 SSE 逐行读一模一样。
- 第一句必须 `hello`，版本不匹配直接 `error{proto_mismatch}` 后断开 —— 不让旧前端和新 daemon 互相猜。
- 单行长度上限 32 MiB（防呆；一份大转录会超过几 MB，所以不是 64 KB 那种玩笑值）。

---

## 3. 消息表（定稿）

**核心思想**：线上传的是**前端要画的东西**，不是服务端内部事件。
所以 `SessionEvent` 那些 `Delta` / `ToolStart` 原样不出门 —— 前端拿到的是
「转录尾巴 + 正在流的那半句 + 状态」。谁要画什么，就传什么。

### 前端 → daemon

| 消息 | 字段 | 说明 |
|---|---|---|
| `hello` | `proto`, `client` | 握手 |
| （无） | — | **没有「新建会话」这条消息**：草稿态下直接 `submit` 即可，daemon 按需建行 |
| `attach` | `id` | 附着（含 resume：语义就是附着到一个已存在的会话） |
| `detach` | — | 离开这个会话（见第 4 节：没人看就停） |
| `list_sessions` | — | 会话列表（给 picker / `mypi sessions`） |
| `list_rounds` | `id` | 某会话的回合头（调试、复现用） |
| `replay` | `id`, `round` | 从库里重建那一轮的请求（只读，不改状态） |
| `submit` | `text` | 开一轮。**草稿态**下提交 → daemon 先建会话行再开跑，并推回 `attached` |
| `interrupt` | — | **任何时刻都能发**，打断当前会话在跑的回合 |
| `logs` | `limit` | 取内存日志（重试/超时那些本地叙述） |
| `quit` | — | 结束当前回合并断开（干净退出用） |

### daemon → 前端

| 消息 | 字段 | 何时发 |
|---|---|---|
| `hello_ok` | `proto` | 握手成功（此时还没有会话） |
| `attached` | `session_id` | 会话确定了：显式 attach，或草稿态首次 submit 时自动建 |
| `transcript` | `entries` | 附着时给一次全量；换分支/换会话时再给一次 |
| `entry` | `entry` | 转录尾巴新增一条（工具请求/结果、回复、错误都在里面） |
| `stream` | `active`, `text`, `reasoning`, `live`, `run_state` | 正在流的那半句；每帧合并发一次（≤ 33ms 一条） |
| `state` | `model`, `cwd`, `name`, `spend`, `usage`, `busy` | 状态栏；变化时发 |
| `sessions` / `rounds` / `replay` | 查询结果 | 对应请求的回包 |
| `logs` | `records` | 日志回包 |
| `error` | `code`, `message` | 协议层错误（非回合错误；回合错误是 `entry`） |

`Change` 不出门：消息本身就是「哪里变了」。

**键鼠事件永远不过 socket**：daemon 只收语义输入（`submit` / `interrupt` / `quit` / 查询）。
键位、焦点、鼠标命中都是前端自己的事 —— 交给服务端就得引入「谁是焦点」这种客户端概念，
凭空多一层状态，没有任何收益。

**转录尾巴怎么算**：daemon 侧记住每个会话上次发出去的
`(transcript_generation, len)`；代变了发全量 `transcript`，否则只发
`entries[len..]`。跟历史区缓存判定完全同构，不需要在服务端维护第二份台账。

---

## 4. 附着、断开、空闲退出

- **附着**：先发一次 `transcript` + `stream` + `state`（快照），之后转增量。
  「生成中途附着上来」就靠这个，不需要任何 replay 把戏。
- **断开**（`detach` / `quit` / 连接断 / 前端崩）：这个会话在跑的回合**立刻打断并定稿**
  （半截正文落盘，`stop_reason = interrupted`）。**没有人在看，就不再跑。**
- 多前端附着同一会话：最后一个离开的人触发停止。
- **空闲退出**：没有附着的前端、没有在跑的回合 → 空闲 N 分钟（默认 10，可配）→ daemon 退出。

---

## 5. 打断（独立路径，按「能收手的地方各自收手」）

协议里 `interrupt` 是一条独立消息，任何时刻都可以发。daemon 侧是一个共享的 **停止句柄**
（一个 `Arc<Interrupt>`：一个 `stopped` 标志 + 正在跑的东西的登记表），
打断 = 置位 + 让每一处**立刻**收手，不许有任何一处等「轮询周期」。

| 环节 | 怎么收手 | 最坏延迟 |
|---|---|---|
| 正在收模型流（正文或思考） | 每个块到达时查停止位 → 丢掉 reader、关 socket（上游写到已关的 socket 就会停） | 一个块的间隔，通常 <100ms |
| 正在跑 bash | 工具线程阻塞在通道上，收到 `Stop` 立刻杀**整个进程组** | 即时（消息一到就动手） |
| 正在跑别的工具（read/edit/浏览器） | 各调用点的检查点看停止位 | 一道调用之内 |
| 等响应头（网关吞了请求但没答） | 无法中途掐断一次阻塞的 read，只能等 ureq 的响应头超时（现设 120s） | **≤120s，这是接受的缺口** |
| 一个字节都不来的静默连接 | 同上：没有块到达就没有检查点 | 同上一格 |

**关于「最多多生成一两个 token」**：上游的打断手段就是断开连接，网关注意到之前可能多吐一两个块。
按现在的按百万 token 计价，这点可以忽略，不值得为它换实现。

**要做的两件事（都不引新依赖、不碰不稳定接口）**：

1. **换掉 bash 的执行方式**（改我们那 40 行调用代码，**不是写 shell**）：
   现在 `Command::new("timeout")…output()` 把子进程吞在里面、超时外包给外部 `timeout(1)`，
   **连 pid 都拿不到**。改成 `spawn()` + 自己的进程组（std 的 `process_group(0)`）+
   一个 waiter 线程阻塞在 `wait_with_output()` 上，结果经通道送回工具线程；
   工具线程 `recv` 到 `Stop` 就整组收。超时也变成我们自己的 deadline（顺带甩掉 `timeout(1)`）。
   **整组怎么杀**：coreutils 的 `kill` 支持负数 pid（= 进程组）——
   `kill -TERM -- -<pgid>`，2 秒后 `kill -KILL -- -<pgid>`；`kill` 不存在就退化成 std 的
   `child.kill()`（只杀直接子进程）。**零新依赖**，和现在用 `date` / `timeout` 是同一个量级。
2. **HTTP 的检查点要加密**：现在只在**正文**块上查停止位，改成**正文和思考块都查**
   （`on_reasoning` 也返回 bool，读到 false 就跳出读循环）。纯增量改动；请求仍由回合线程做
   （不另起线程、不碰 ureq 内部）。

**std 没有 `select`**，所以「同时等子进程退出、等停止」不要写成循环：让 waiter 线程和打断路径
往**同一个通道**发带标签的消息（`Exited` / `Stop`），工具线程只 `recv`。零轮询。

**不接受的做法**（写下来防止将来有人又想加）：为了「掐断阻塞读」去实现 ureq 的 `Connector`
（位于 `unversioned`，上游明确不保证 API 稳定），或为此维护一张 fd 注册表 ——
收益只是「省掉一两个块」，代价是一层不稳定依赖，不值。

## 6. 线程模型（全是阻塞，没有 async）

### daemon

```
        连接读线程 ×N（阻塞读 socket → 解析 → 发命令）
                  │  mpsc<Command>
                  ▼
        daemon 主线程（唯一持有 SessionHub + SQLite）
          · recv_timeout(命令, 5ms) → 处理命令
          · hub.drain_all() → 按会话 id 路由到各连接的写出队列
                  │
                  ▼
        连接写线程 ×N（阻塞写 socket，慢死也只慢它自己）
                  ▲
        回合线程 ×M（已有：一个回合一个线程，负责 HTTP 与工具）
```

- 主线程 = 数据库的**唯一使用者**，跟今天同一条纪律（写者唯一，不上锁）。
- 回合线程只把事件交给 hub（已有路径），不直接碰 socket。

### 前端（TUI）

```
socket 读线程（阻塞读 → 翻译成 Signal）─┐
键盘线程（已有）───────────────────────┼→ 唯一信号通道 → 主循环 → 子区
resize（crossterm）────────────────────┘
```

前端不引 async 运行时；线程只是「阻塞读」，和现在键盘线程一个性质。

---

## 7. 背压（慢前端不许拖住回合）

为什么需要：回合线程若直接阻塞写 socket，前端一打嗝 → socket 缓冲区满 →
回合线程卡住 → 不再读网关的流 → **网关继续吐 token，白烧钱**。

做法：每个连接一个队列 + 一个写线程，消息分两类：

| 类别 | 内容 | 满了怎么办 |
|---|---|---|
| 可合并 | `stream`（文本增量） | **合并，不丢内容**：追加进每连接一个文本缓冲，排队的多条小块丢掉，写线程追上来时一次性发一大块。打字机变粗，文字一字节不少。 |
| 不许丢 | `entry` / `transcript` / `state` / 回包 / `error` | 无界队列（一轮里就几十条、每条几百字节）。**上限 10000 条**，超过判定前端已死 → 断开它（断开落回第 4 节：没人看就停）。 |

写 socket 直接失败（EPIPE）= 断开。**不会因为「慢」而断开**，只有「死」才断。

---

## 8. 模块落地

**新增**

```
src/server/daemon.rs     监听、连接线程、主循环、附着表、背压队列、空闲退出
src/server/wire.rs       消息类型（serde）+ 编解码（行分隔 JSON）+ proto 版本
src/server/interrupt.rs  停止句柄：标志 + 「正在跑的东西」登记表 + 各处的动手方式
src/tui/session/client.rs  前端侧：连 socket、握手、读线程、把消息翻译成子区调用
```

**改**

- `server/hub.rs`：`drain` 之外再暴露「转录尾巴 + stream 快照」给路由用（主线程内调用，不跨线程）。
- `cli.rs`：加 `--server`、位置子命令 `attach <id>` / `sessions`（首次引入位置参数；不猜缩写）。
- `server/agent/*`：bash 从 `output()` 改成 `spawn()` + 独立进程组 + waiter 线程；工具拿停止句柄。

**删**

- `tui/session/loop.rs` 里所有直接摸库/摸网络的代码：`Store::open`、`db_path`、建 `Client`、读 provider 与密钥。
- `Signal::Session(SessionEvent)` 那条 in-process 通路（被 socket 消息取代）。
- 前端对 `server::store` / `server::ai::client` 的 import。

**不动**

- TUI 三个子区（历史 / 输入 / 保留）：它们只认 `push_entry` / `replace_transcript` / `notify` / `render_rows`，协议消息正好翻译成这些调用。
- `store` / `rounds` / `replay_round` / 复现那套契约。
- `ARCHITECTURE.md` 里那条分层守卫（daemon 也在 `server::` 下，规则自动适用）。

---

## 9. 落地顺序（每步都能单独验证）

| 步 | 做什么 | 怎么验 | 估时 |
|---|---|---|---|
| 1 | `wire.rs`：消息类型 + 编解码 | 单测：往返、半个消息分批到达、超长行拒绝、坏 JSON 只断这一条连接 | 半天 |
| 2 | `daemon.rs`：监听 + 连接线程 + 主循环 + 附着表 + 背压 | 集成测：两个真 socket 客户端同时附着两个会话，一个故意不读，断言回合不受影响、恢复后文本完整 | 1–1.5 天 |
| 3 | 打断落地（停止句柄 + bash 执行方式改造 + 思考块检查点） | 测：纯思考流中打断（≤一个块）、bash `sleep 999` 中打断（即时、整组收）、半截落盘 | 0.5–1 天 |
| 4 | CLI + 自动拉起 + 握手 | 冒烟：`mypi --server` → `mypi sessions` → `mypi attach` 手撸一遍 | 半天 |
| 5 | TUI 换连接客户端 | 现在那 559 条测试里跟子区有关的全部不动；新增「收到消息 → 子区状态」的翻译测试 | 1 天 |
| 6 | 清死代码 | 编译 + 架构守卫 + 全量测试 | 半天 |

合计约 **4.5–5.5 天**，每步结束都可以停下让你看。

---

## 10. 还没定的（明说，别当已定）

1. **主题归谁**：我按第 0 节的默认写（前端本地读，其余配置 daemon 独占）。你要主题也走协议下发，我改。
2. **`attach` 后能不能换会话**：现在设计成「换 = detach + attach」。要不要一条 `switch` 消息省一次往返，看你。
3. **流式 live tail 在协议里的表示**：`stream` 消息只带「正在流的那半句」；历史区怎么把 live 尾巴拼进窗口（切窗前预留行数那条铁律）是 TUI 侧的事，协议不动。这条等做流式时再定。
4. **无人值守跑批**（没有前端也想让回合跑完）：按你的话现在不做；将来加显式开关。

已定，不再讨论：懒建会话（§1）、键鼠不过 socket（§3）、打断按「各环节各自收手」做、不引新依赖（§5）。

**接受的缺口**（写清楚，免得以后被当成 bug 追）：网关吞了请求却不回响应头、或连接彻底静默时，
停止位没有检查点，Esc 要等到 ureq 的响应头超时（现设 120s）才生效。这两格没有 token 在烧，
真嫌久就把响应头超时调小（一行 config），不必为它上 fd 方案。
